# Finding closure (release ledger)

This directory is the v2 **release** closure ledger. It does not replace
`docs/pi-context-runtime-v2-audit-rearchitecture-plan-v1.0.0/findings/findings.json`.

T54 records F017 and F029 as closed with pointers to the commits and hashes
in `release/manifest.json`. Other findings stay open in the plan ledger until
a later explicit close.
