===== A05 =====
# A05 — 启用 Branch Protection 与 Required Checks

## 元数据

- Wave：`W0`
- Depends on：A04
- Findings：F101, F123
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

main 必须由 required checks 保护，advisory/nightly 与 required 分离。

## 允许修改

- `.github/workflows/**`
- `docs/OPERATIONS.md`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. required job 汇总所有非 advisory cell
2. 配置 codeowners/review
3. 记录 GitHub protection evidence

## 必须先写的 RED 测试

- failed unit blocks merge
- skipped required is fail
- advisory does not block

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
node scripts/ci/verify-protection.mjs
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

API 显示 protection enabled，required contexts 与文档一致。

## Evidence

```json
{
  "taskId": "A05",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A05/red.log",
  "greenLog": "artifacts/task-evidence/A05/green.log",
  "fullGateLog": "artifacts/task-evidence/A05/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F101", "F123"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a05): 启用 Branch Protection 与 Required Checks"
```
===== A08 =====
# A08 — 让默认 Extension 只使用 ProductionRuntimeCompositionRoot

## 元数据

- Wave：`W1`
- Depends on：A04, A07
- Findings：F105
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

删除 default `bindClaimedRuntime` 中 ad-hoc 子系统 new；所有 Hook 路由到 root/session。

## 允许修改

- `apps/pi-context-runtime/src/extension.ts`
- `apps/pi-context-runtime/src/composition-root.ts`
- `tests/acceptance/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. Production root 注册全部 hooks
2. 公开 factory 与 packed entry 使用相同 root
3. fixture factory 移入 testkit

## 必须先写的 RED 测试

- one owner only
- no direct materializer/compactor in adapter
- packed root path

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/acceptance/product-runtime-path.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

静态扫描证明 adapter 只调用 RuntimeSession methods。

## Evidence

```json
{
  "taskId": "A08",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A08/red.log",
  "greenLog": "artifacts/task-evidence/A08/green.log",
  "fullGateLog": "artifacts/task-evidence/A08/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F105"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a08): 让默认 Extension 只使用 ProductionRuntimeCompositionRoot"
```
===== A12 =====
# A12 — 实现统一 Runtime Snapshot Transaction

## 元数据

- Wave：`W1`
- Depends on：A11
- Findings：F106, F109
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

一次事务读取 directives/claims/continuity/catalog/recent source span，产生 snapshotHash。

## 允许修改

- `packages/runtime/src/snapshot/**`
- `packages/storage-node/src/sqlite-store.ts`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. SQLite read transaction
2. bind Pi firstKept/source entries
3. record heads/schema/config revisions

## 必须先写的 RED 测试

- concurrent ingress snapshot isolation
- branch change stale
- same snapshot hash two-run

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/runtime/test/runtime-snapshot.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Materialize/Checkpoint 同一 boundary 得到相同 snapshotHash/heads。

## Evidence

```json
{
  "taskId": "A12",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A12/red.log",
  "greenLog": "artifacts/task-evidence/A12/green.log",
  "fullGateLog": "artifacts/task-evidence/A12/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F106", "F109"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a12): 实现统一 Runtime Snapshot Transaction"
```
===== A14 =====
# A14 — 接入真实 Route/System/Tools Budget Port

## 元数据

- Wave：`W1`
- Depends on：A09
- Findings：F139
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

I_eff 包含实际 model limits、system/tools/image/provider reserve。

## 允许修改

- `packages/pi-adapter/src/**`
- `packages/core/src/budget/**`
- `packages/runtime/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 从 Pi public ctx/envelope 获取 limits
2. serialize tools/system payload token
3. provider route calibration
4. unknown route conservative fail

## 必须先写的 RED 测试

- tool-heavy budget
- image block
- model switch invalidates calibration

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/core/test/budget/** tests/pi-contract/**
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Materializer estimate 与 serialized payload误差在预注册范围内。

## Evidence

```json
{
  "taskId": "A14",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A14/red.log",
  "greenLog": "artifacts/task-evidence/A14/green.log",
  "fullGateLog": "artifacts/task-evidence/A14/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F139"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a14): 接入真实 Route/System/Tools Budget Port"
```
===== A21 =====
# A21 — 实现 Proactive Recall 与 Purpose-bound Lease

## 元数据

- Wave：`W2`
- Depends on：A11, A19
- Findings：无直接 Finding；属于 Gate/集成任务
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

每个 user turn 轻量 query，必要时注入，非必要保持沉默。

## 允许修改

- `packages/runtime/src/retrieval/**`
- `packages/core/src/retrieval/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. query from goals/paths/errors/tools
2. exact→literal→FTS
3. lease bound to unresolved action
4. dedup/cooldown

## 必须先写的 RED 测试

- recall-needed
- recall-not-needed
- stale conflicting evidence

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/runtime/test/proactive-recall.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

recall@5/precision/silence 达预注册 W1 门。

## Evidence

```json
{
  "taskId": "A21",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A21/red.log",
  "greenLog": "artifacts/task-evidence/A21/green.log",
  "fullGateLog": "artifacts/task-evidence/A21/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": []
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a21): 实现 Proactive Recall 与 Purpose-bound Lease"
```
===== A30 =====
# A30 — Tool-pair/Retained-tail/Two-run Hash Verifier

## 元数据

- Wave：`W3`
- Depends on：A24, A28
- Findings：F110
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

从真实 Pi messages 计算，不写死。

## 允许修改

- `packages/core/src/compaction/**`
- `packages/pi-adapter/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. parse toolCall/result
2. atomic cut
3. retained tail exact IDs
4. render twice fresh process

## 必须先写的 RED 测试

- orphan result/call
- split turn
- nondeterministic ordering

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/integration/checkpoint-hard-gates.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

hard metrics来自 verifier receipt，0/1不能由 runner常量填充。

## Evidence

```json
{
  "taskId": "A30",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A30/red.log",
  "greenLog": "artifacts/task-evidence/A30/green.log",
  "fullGateLog": "artifacts/task-evidence/A30/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F110"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a30): Tool-pair/Retained-tail/Two-run Hash Verifier"
```
===== A34 =====
# A34 — 实现真实 Pi B0/B1/B2 Runner

## 元数据

- Wave：`W4`
- Depends on：A29, A30, A33
- Findings：F117, F118
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

替换 fake Native/string marker。

## 允许修改

- `packages/benchmark/src/arms/**`
- `tests/live-gate/**`
- `scripts/benchmark/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. spawn real Pi per arm
2. B0 no extension or ingress-only profile
3. B1 checkpoint identity context
4. B2 full materializer/recall
5. same model/config/seed

## 必须先写的 RED 测试

- arm identity proof
- same cut/store
- extension ownership

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:w2:smoke
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

run bundle证明 B0 fromHook=false、B1/B2真实 hook，且 B2非字符串模拟。

## Evidence

```json
{
  "taskId": "A34",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A34/red.log",
  "greenLog": "artifacts/task-evidence/A34/green.log",
  "fullGateLog": "artifacts/task-evidence/A34/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F117", "F118"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a34): 实现真实 Pi B0/B1/B2 Runner"
```
===== A42 =====
# A42 — 运行 Boundary Replay 30 Cluster × 3 Seeds

## 元数据

- Wave：`W5`
- Depends on：A41
- Findings：F124
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

第一轮真实 Gate smoke。

## 允许修改

- `artifacts/runs/w2-v3/boundary/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. clean tree/container
2. 90 paired continuations
3. all failures retained
4. family report

## 必须先写的 RED 测试

- infrastructure exclusion policy
- seed reproducibility

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:w2:boundary --clusters 30 --seeds 3
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Hard 通过后才解读 Quality/Efficiency；否则生成 root-cause bundle。

## Evidence

```json
{
  "taskId": "A42",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A42/red.log",
  "greenLog": "artifacts/task-evidence/A42/green.log",
  "fullGateLog": "artifacts/task-evidence/A42/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F124"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a42): 运行 Boundary Replay 30 Cluster × 3 Seeds"
```
===== A43 =====
# A43 — 运行 Natural 200K Threshold Lane

## 元数据

- Wave：`W5`
- Depends on：A42
- Findings：F125
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

默认 Pi keepRecent/reserve 自动触发。

## 允许修改

- `artifacts/runs/w2-v3/natural-threshold/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 真实 tool-heavy task增长
2. 不手工 compact
3. 记录 payload/cache/trigger
4. same task arms

## 必须先写的 RED 测试

- no trigger invalid run
- early trigger
- large turn

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:w2:natural-threshold
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

至少每 arm真实 threshold一次，cut/quality可比较。

## Evidence

```json
{
  "taskId": "A43",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A43/red.log",
  "greenLog": "artifacts/task-evidence/A43/green.log",
  "fullGateLog": "artifacts/task-evidence/A43/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F125"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a43): 运行 Natural 200K Threshold Lane"
```
===== A44 =====
# A44 — 运行 Provider Overflow Lane

## 元数据

- Wave：`W5`
- Depends on：A42
- Findings：F125
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

真实 overflow→compact→retry progress。

## 允许修改

- `artifacts/runs/w2-v3/overflow/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. provider error capture
2. request hash progression
3. Native/PCR retry
4. no infinite loop

## 必须先写的 RED 测试

- checkpoint reject
- corrupt pointer
- retry overflow

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:w2:overflow
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

每次 retry output hash变化且 tokens严格下降，任务质量非劣。

## Evidence

```json
{
  "taskId": "A44",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A44/red.log",
  "greenLog": "artifacts/task-evidence/A44/green.log",
  "fullGateLog": "artifacts/task-evidence/A44/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F125"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a44): 运行 Provider Overflow Lane"
```
===== A45 =====
# A45 — 运行 Recursive Long-horizon Lane

## 元数据

- Wave：`W5`
- Depends on：A42, A43
- Findings：F122, F126
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

三次压缩、branch、correction、recall、restart。

## 允许修改

- `artifacts/runs/w2-v3/recursive/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. task fronts
2. temporal update after compact1
3. branch after compact2
4. restart before compact3
5. recall needed/not-needed

## 必须先写的 RED 测试

- old directive survival
- side effect replay guard

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:w2:recursive
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

所有 hard pins/side effects/next action跨三轮正确。

## Evidence

```json
{
  "taskId": "A45",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A45/red.log",
  "greenLog": "artifacts/task-evidence/A45/green.log",
  "fullGateLog": "artifacts/task-evidence/A45/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F122", "F126"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a45): 运行 Recursive Long-horizon Lane"
```
===== A48 =====
# A48 — 执行 W1/W2 Deterministic Release Gate

## 元数据

- Wave：`W5`
- Depends on：A42, A43, A44, A45, A46, A47
- Findings：F135
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

使用规范 Gate v3 生成唯一决策。

## 允许修改

- `artifacts/gates/deterministic-v3/**`
- `scripts/gates/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. verify current CI/protection
2. validate bundles
3. hard/quality/efficiency
4. derive publicationClaim false/true

## 必须先写的 RED 测试

- missing lane blocks
- stale commit blocks
- family regression blocks

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm gate:deterministic-v3
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

输出 adopt/keep-native/stop，理由与全部 bundle hash绑定。

## Evidence

```json
{
  "taskId": "A48",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A48/red.log",
  "greenLog": "artifacts/task-evidence/A48/green.log",
  "fullGateLog": "artifacts/task-evidence/A48/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F135"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a48): 执行 W1/W2 Deterministic Release Gate"
```
===== A49 =====
# A49 — 完成 Hermetic Package/Release Matrix

## 元数据

- Wave：`W5`
- Depends on：A05, A06, A48
- Findings：F103, F132
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

当前 commit tarball在空 HOME、Linux/macOS、Node/Pi matrix验证。

## 允许修改

- `artifacts/release/**`
- `scripts/release/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. loader-only no credentials
2. live provider separate
3. SBOM/license/compat/rollback
4. manifest embeds gate

## 必须先写的 RED 测试

- stock Pi rejection/patched host acceptance
- uninstall/reinstall
- no external TS imports

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm release:verify
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

所有 required CI green 后才产 release candidate。

## Evidence

```json
{
  "taskId": "A49",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A49/red.log",
  "greenLog": "artifacts/task-evidence/A49/green.log",
  "fullGateLog": "artifacts/task-evidence/A49/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F103", "F132"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a49): 完成 Hermetic Package/Release Matrix"
```
