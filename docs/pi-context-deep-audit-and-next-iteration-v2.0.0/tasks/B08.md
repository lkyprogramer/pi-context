# B08 — 所有 User/Tool Ingress 必须调用 RuntimeSession

## 元数据

- Wave：`W1`
- Depends on：B03
- Findings：NF004
- Completion：RED、GREEN、NARROW、FULL-GATE、Evidence Seal、当前 HEAD 绑定必须同时成立。

## 目标

Service resolver 返回 RuntimeSession facade，user input 调 ingestUserInput，tool result 调 ingestToolResult，禁止直接 owner.service/observation。

## 允许修改

- `apps/pi-context-runtime/src/composition-root.ts`
- `packages/pi-adapter/src/*hook*`
- `tests/acceptance/product-runtime-path.test.ts`

禁止扩大 Scope。发现依赖缺口时写 blocker，并由 Controller 生成新的显式任务；不得在本任务中顺手改动其他生产层。

## 执行步骤

1. RED：并发 ingress+compaction 必须按 RuntimeSession write chain 顺序
2. 静态检查默认 Extension 不调用底层 Store/Service
3. 保留 idempotency/abort 语义

## RED 测试要求

- 先写能稳定复现缺陷的测试；失败必须来自目标行为，不得来自缺依赖、路径或凭证。
- 对 concurrency/time/network 相关任务必须使用可控 Clock、Barrier、Recorded Provider Response 或独立 Live Lane。
- 测试名称不得使用 `live`，除非真实启动 Pi、真实加载当前 tarball/extension 并调用真实 Provider。

## 验收命令

```bash
pnpm vitest run tests/acceptance/product-runtime-path.test.ts tests/acceptance/runtime-session-ownership.test.ts
```

## 验收不变量

1. 所有 Findings 的 observation 均有直接反例测试。
2. 不允许常量指标、字符串 Stub、预编程正确 Executor 或把缺失证据记为 0。
3. Abort/timeout/scope mismatch 必须 typed fail；失败样本保留。
4. Full Gate 必须在干净树、当前 HEAD、固定依赖与独立临时目录运行。

## Evidence Seal v2

```json
{
  "schemaVersion": 2,
  "taskId": "B08",
  "status": "done",
  "currentHead": "<40-hex>",
  "allowedDiffSha256": "<sha256>",
  "red": {"command":"...","exitCode":1,"logSha256":"..."},
  "green": {"command":"...","exitCode":0,"logSha256":"..."},
  "fullGate": {"command":"...","exitCode":0,"logSha256":"..."},
  "runBundleHashes": ["<sha256>"],
  "acceptanceAssertions": [{"id":"...","ok":true,"evidence":"..."}],
  "findingsClosed": ["NF004"],
  "dirty": false
}
```

## Commit 规则

```bash
git status --short
git diff --check
git add -- <allowed files + tests + evidence>
git commit -m "fix(b08): 所有 User/Tool Ingress 必须调用 RuntimeSession"
```
