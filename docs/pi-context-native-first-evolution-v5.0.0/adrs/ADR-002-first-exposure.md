# ADR-002 · 首次可见不做有损观察压缩
状态：接受为v5目标。依据：当前A02/A04/A08、S14、S19。

决定：tool_result原样返回；context投影只针对同代成功请求暴露过的旧纯文本观察，未知与图片保留，完整batch保护。代价：首个超大工具结果仍可能触发宿主原生窗口限制；不伪称完全避免溢出。

替代：首次前摘要可更早省token，但可能在模型尚未知道要回读时丢关键事实。未来只有模型/工具专属消融证明可靠且用户明确开启才考虑，不是默认。验收：T10–T14、INV-01…04。
