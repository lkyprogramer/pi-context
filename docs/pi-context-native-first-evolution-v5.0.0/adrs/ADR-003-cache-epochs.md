# ADR-003 · 冻结epoch替代每轮滑窗改写
状态：接受为v5目标。依据：S14、S21。

决定：同一epoch替换位置与stub字节固定；新增消息原样追加。记录首次变更prefix和真实总成本，触发新epoch必须有明确边界或收益条件。代价：减少冗余不是即时最大化，短期token数可能较高。

替代：每轮最小化token会造成持续prefix变化，可能增大总prefill与账单。验收：T13/T16、INV-07、B2 vs B1。若无法测出净收益，默认回到observe。
