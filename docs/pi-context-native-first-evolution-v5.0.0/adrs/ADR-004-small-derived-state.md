# ADR-004 · 本地派生索引，不默认摘要DAG／跨项目记忆
状态：接受为v5目标。依据：S08、S15、S24。

决定：原生branch＋source refs＋有限FTS＋显式pin；SQLite仅派生，可重建。没有第二份完整会话日志、嵌入数据库服务或默认Observer。代价：跨episode抽象查询可能不如层级摘要，需要C2和实际失败族证明后再加。

验收：T05–T09、T15。扩展条件：FTS失误确实成为主要失败原因且新增层在B3消融中有净收益。
