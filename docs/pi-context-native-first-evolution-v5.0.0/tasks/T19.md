# T19 · 工具／命令接线与真实打包

状态：**待实施**。里程碑：M3。前置任务：T08, T09, T14, T15, T16。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `src/commands.ts`
- `src/extension.ts`
- `scripts/check-public-imports.mjs`
- `scripts/packed-host.mjs`
- `test/packed/install.test.ts`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

只暴露pctx_history和/pctx，安装不包含测试或旧PCR补丁。

1. off/observe不改变provider消息；模式切换schema保持稳定。
2. TUI/RPC/print无UI确认时不伪造pin确认。
3. tarball Worker路径可运行，干净官方宿主load/resume/uninstall成功。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 只组合已经实现的核心模块；T17/T18未实现时experimental-semantic必须返回unsupported，不暴露假能力。实现profile、status、doctor、pin/unpin、无内容export。
2. 固定公开工具参数和描述，拒绝未知字段／scope参数。
3. 真实npm pack后隔离安装，生成导入边界与内容清单检查。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/packed/install.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T19/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

不发布npm；src/testing、fixtures、会话原文、旧文档不得进入运行时包。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
