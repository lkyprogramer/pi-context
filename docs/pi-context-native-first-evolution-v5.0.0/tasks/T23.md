# T23 · Java场景oracle与真实模型C2

状态：**待实施**。里程碑：M3。前置任务：T21, T22。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `eval/cases/`
- `eval/oracles/`
- `eval/recovery-probe.ts`
- `test/integration/java-cases.test.ts`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

核心8案例有确定失败/成功oracle；恢复nonce只能从native历史取得。

1. 有缺陷Java基线应失败，已知正确实现应通过；防止oracle恒真。
2. 真实model调用history并正确使用nonce才能C2-pass。
3. artifact缺失、测试没运行、修改禁止文件都不能task-pass。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 按cases/J01–J08生成可复现起始仓库与外部grader。
2. 接入实际Maven/Spring任务和多阶段压缩，不把reader当coding。
3. 加入stale-success、branch-conflict、不可重建原文与长日志故障分类。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/integration/java-cases.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T23/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

本包Java fixtures是起点；未落地的Spring或RPC驱动不能伪称已经完成。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
