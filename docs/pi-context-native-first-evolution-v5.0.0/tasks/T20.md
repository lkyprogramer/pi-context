# T20 · 性质／故障／安全矩阵

状态：**待实施**。里程碑：M3。前置任务：T19。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `test/property/invariants.test.ts`
- `test/fault/runtime.test.ts`
- `test/security/injection.test.ts`
- `test/host/matrix.test.ts`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

以真实host路径和故障注入验证适用的INV-01至INV-10，不以mock绿色代替；语义stage/ACK部分仅在T17/T18实现后运行，核心先验证native事件后胶囊激活。

1. 所有protocol向量覆盖；A01–A08转为应不再出现的v5回归。
2. 故障点throw/timeout/kill/generation-change四类扰动有记录。
3. 100k历史冷建库不阻塞context，warm p95和RSS单独报告。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 建立固定seed的生成式测试，保存失败seed。
2. 运行完整事件矩阵、路径注入、跨分支、错误恢复。
3. 将测试数量、实际执行列表和skip原因一起输出JSON。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/property test/fault test/security test/host/matrix.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T20/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

没有实际执行的测试不得标pass；性能目标未达先优化实现不删安全检查。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
