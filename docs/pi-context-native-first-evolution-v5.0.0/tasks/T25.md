# T25 · 切换唯一入口并删除旧运行时负担

状态：**待实施**。里程碑：M3。前置任务：T19, T20。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `package.json`
- `pnpm-workspace.yaml`
- `patches/`
- `apps/pi-context-runtime/`
- `packages/`
- `README.md`
- `AGENTS.md`
- `docs/`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

清理后仍能在官方Pi完成安装／恢复，旧用户数据完全不动。

1. 产物无PCR host patch、跨宿主kernel或benchmark生产依赖。
2. 历史资料标superseded而非与v5并行生效。
3. 卸载v5后native session可以继续，不依赖新库反向迁移。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 先保存基线和故障样本，再逐层删除旧生产入口。
2. 只保留真正复用且符合新契约的代码和测试，不保留空转接口层。
3. 写唯一安装、运维、冲突说明和数据不迁移政策。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/packed/install.test.ts test/host/matrix.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T25/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

不能删除用户数据、未提交修改或生产配置；不要仅重命名旧包假装瘦身。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
