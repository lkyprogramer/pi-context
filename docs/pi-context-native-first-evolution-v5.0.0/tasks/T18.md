# T18 · 可选语义压缩及B3消融

状态：**待实施**。里程碑：M4。前置任务：T17, T16, T24。这是可选M4任务，不作为核心版本发布的强制前置。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `src/checkpoint/semantic.ts`
- `test/host/semantic.test.ts`
- `eval/semantic-arm.ts`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

semantic默认关闭；只有explicit profile且路由授权时生成受预算候选。

1. 包括messagesToSummarize/turnPrefix/previousSummary，保留公开firstKept边界。
2. 返回toolCall、来源造假、不缩短、超时或用户取消时不staging。
3. B3统计辅助模型全部usage，不能把压缩更短当自动胜出。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 接入公开session_before_compact，单次生成＋最多一次有限重试。
2. 经过validator和T17后提交，原生失败路径仍由Pi负责。
3. 在B2已完成评价后才执行B3消融，无净增益则不推荐默认。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/host/semantic.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T18/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

此任务可延期，不能阻断history/balanced正确性发布；不得引入专属fork hooks。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
