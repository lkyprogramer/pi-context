# T24 · 运行评价与预注册晋级判断

状态：**待实施**。里程碑：M3。前置任务：T20, T22, T23。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `eval/report.ts`
- `eval/scorers.ts`
- `test/unit/report.test.ts`
- `artifacts/v5-evaluation/`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

报告同时呈现ITT、完整配对、独立cluster、C1/C2/E2E及未知费用。

1. 8cluster全成功不能声明2%NI。
2. synthetic-example不得混入real-run主结论。
3. 同任务多seed聚类，critical violation单独阻断。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 先冻结主要资源指标和非劣政策，再跑personal smoke。
2. 分析失败族与B0/B1/B2差别，必要时保持observe。
3. 输出证据矩阵、完整结果和未测试范围，不只输出一个绿色badge。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/unit/report.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T24/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

真实调用需要已配置provider和预算；没有结果则NOT-RUN，不编造预期提升百分比。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
