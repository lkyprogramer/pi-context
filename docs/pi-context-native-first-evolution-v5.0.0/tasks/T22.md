# T22 · 同宿主配对runner与不可变run manifest

状态：**待实施**。里程碑：M3。前置任务：T19, T21。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `eval/runner.ts`
- `eval/pairing.ts`
- `eval/manifest.ts`
- `test/integration/eval-runner.test.ts`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

B0/B1/B2同宿主同起点，arm轨迹独立，所有计划项保留分母。

1. AB/BA顺序冻结；超时或provider异常仍留下task/arm状态。
2. B0不加载插件，B1仅observe；候选配置可核查。
3. 达到总墙钟／调用预算后剩余incomplete，不继续无限执行。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 实现fixed-prefix replay与closed-loop两种独立lane。
2. 记录源码/模型/价格/镜像/seed/oracle身份及原始usage。
3. 真正启动Pi RPC并消费完成事件，不只构造mockmessages。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/integration/eval-runner.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T22/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

旧v4不同宿主仅diagnostic；不向被测模型泄露oracle和上个arm答案。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
