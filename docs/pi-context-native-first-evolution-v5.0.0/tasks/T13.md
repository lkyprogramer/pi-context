# T13 · 稳定epoch规划器

状态：**待实施**。里程碑：M2。前置任务：T06, T10, T11, T12。

## 上下文与输入

先读`AI-START-HERE.md`、`docs/05-target-architecture.md`、`docs/06-pi-contract.md`和`contracts/model.ts`。本任务在目标仓库实现，不在本设计zip里假造产品。固定官方Pi与原生会话事实源约束不可更改。

## 唯一写入责任

- `src/projection/planner.ts`
- `test/unit/planner.test.ts`

存在公共文件后续任务接管时，必须在前置任务完成后串行修改；并行Agent不能同时写同一文件。

## RED：先写可失败的行为测试

同epoch新消息只追加；没有足够收益不改旧prefix。

1. F06旧generation计划不能应用。
2. 重复渲染planHash相同；新增raw消息不改变旧stub。
3. 未暴露read尾部marker仍在；invalid ref候选被排除。

先确认测试因目标行为尚未实现而失败，而不是文件拼错、测试没有被发现或依赖缺失。基础模块缺失时先放最小可编译接口，再观察断言RED，不把import错误当功能测试证据。

## GREEN：最小实施步骤

1. 实现候选资格和保护优先排序。
2. 固定replacement集合、source boundary、计划hash。
3. 按配置记录触发原因；native compact或scope变更失效，普通追加不重复规划。

完成后删除本任务内部重复状态和冗余转发，不增加不在任务范围内的策略。修改契约需要同步更新受影响任务与测试，不能用类型断言绕过。

## 验证命令

```bash
pnpm exec vitest run test/unit/planner.test.ts
pnpm exec tsc --noEmit
```

命令在T01/T02初始化的目标仓库中运行。依赖、真实host或沙箱缺失时记录BLOCKED及实际错误；不得用本包文档校验替代这些命令。

## 输出与验收

提交最小代码diff、RED与GREEN原始日志、执行环境／commit／配置hash、测试数量和跳过原因，保存到`artifacts/v5-tasks/T13/`。记录每条验收的passed/failed/incomplete，而非只写“任务已完成”。涉及host的结论须明确是否真实loader/RPC；mock仅可证明单元层。

## 禁止与停止条件

不在context内调用LLM；参数只是实验种子不是宣传的最优阈值。

遇到来源授权、图片保留、first-exposure、batch完整性或native所有权冲突时，以保留原生行为为优先；暂停该优化，不吞用户输入，不扩大权限。
