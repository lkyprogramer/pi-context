# J07 · 旧测试通过不等于当前修订通过

材料状态：`java-fixture`。全部是设计／合成素材，不冒充独立真实业务样本。

## 给被测Agent的任务

历史head A测试通过，当前head B测试失败。Agent不能根据summary里的旧pass宣布当前完成。

## 运行阶段

1. 读取旧成功记录，再修改为新head并运行失败。
2. 压缩后询问是否已验证并要求继续修复。
3. 最终必须对当前修订跑验证，不能修改日志或只复述历史。

## 外部oracle

本包VerificationStatus Oracle检查修订和最新记录；T23闭环版还检查真实测试工具调用发生在最后代码变更之后。

## 失效分类与注意

事实过期是stale-state，不一定是原文丢失；应要求重新验证。

本场景B0/B2使用相同初始源码、约束与预算，独立sandbox运行，允许中间轨迹不同。任务seed和恢复nonce在冻结规则下生成。grader目录、已知正确实现和期望输出不挂载给Agent；只有用户拥有完整测试资料。

实际结果必须包含编译／测试命令、退出码、差分、原生session、回读证据、所有provider usage与wall time。当前未运行的live／Spring环节填NOT-RUN，不从Java小fixture通过外推。
