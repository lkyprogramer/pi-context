# J02 · MyBatis式租户条件不得漏加

材料状态：`java-fixture`。全部是设计／合成素材，不冒充独立真实业务样本。

## 给被测Agent的任务

把一个缺少tenant谓词的查询修复，同时保留id过滤。实际扩展版本接入MyBatis SQL与测试数据库。

## 运行阶段

1. 在同id跨租户数据中定位泄露。
2. 压缩后继续修改并确保list和单条查询均隔离。
3. 不得通过删除测试数据、硬编码租户或关闭查询“通过”测试。

## 外部oracle

本包Java Oracle检查返回行的租户及单条查询；T23扩展数据库版检查真正SQL条件和写入范围。

## 失效分类与注意

协议回读成功但SQL仍错误，是E2E失败，不计context工具成功抵消。

本场景B0/B2使用相同初始源码、约束与预算，独立sandbox运行，允许中间轨迹不同。任务seed和恢复nonce在冻结规则下生成。grader目录、已知正确实现和期望输出不挂载给Agent；只有用户拥有完整测试资料。

实际结果必须包含编译／测试命令、退出码、差分、原生session、回读证据、所有provider usage与wall time。当前未运行的live／Spring环节填NOT-RUN，不从Java小fixture通过外推。
