# F016 closure evidence

Package remains `private`/`UNLICENSED`; INSTALL/CHANGELOG state local tarball / `pi install` only, `npmPublish: false`.
The original conflict (docs advertising npm publish) is gone.

- `apps/pi-context-runtime/package.json` `pcrRelease.npmPublish=false`
- `docs/INSTALL.md`
