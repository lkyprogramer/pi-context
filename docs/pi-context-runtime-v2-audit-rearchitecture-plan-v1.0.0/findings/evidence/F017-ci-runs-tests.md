# F017
compatibility.yml runs frozen-lockfile install, typecheck, unit, acceptance, package-build, pi-contract. compatibility-required aggregates non-advisory cells.
- .github/workflows/compatibility.yml
