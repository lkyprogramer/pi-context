# S06 · W lane（warm）与 X lane（long）场景、seed、oracle

依赖：S03、S04。实现前读取 [目标设计 §6、§8](../design/00-target.md)、[合同](../design/01-contracts.md) 的 `plan/cases.json/RegimeSummary`、[场景清单](../testing/scenarios.json)。关闭 G10、G11、G12。

## 文件范围

- `eval/local/cases.json`
- `eval/local/cases/W-Q01/TASK.md`、`eval/local/cases/W-Q03/TASK.md`、`eval/local/cases/W-Q05/TASK.md`、`eval/local/cases/W-Q07/TASK.md`、`eval/local/cases/X01/TASK.md`
- `eval/local/review-fixtures/W-Q01.json`、`eval/local/review-fixtures/W-Q03.json`、`eval/local/review-fixtures/W-Q05.json`、`eval/local/review-fixtures/W-Q07.json`
- `eval/local/review-seed.mjs`
- `eval/local/review-spec.mjs`
- `eval/local/scenarios.mjs`
- `eval/local/review-matrix.json`
- `eval/local/log-workspace.mjs`（新建，从 `run-episode.mjs` 的 `setupH03Workspace` 提取并参数化）
- `eval/local/run-episode.mjs`
- `eval/local/run-matrix.mjs`
- `eval/local/gate.mjs`
- `eval/local/report.mjs`
- `test/unit/review-scenarios.test.ts`
- `test/unit/review-live-wiring.test.ts`
- `test/unit/local-cases.test.ts`
- `test/host/warm-fold-regime.test.ts`（新建）

## 接口合同

**Consumes：** `buildReviewSeed(fx, opts)`、`SeedBuilder`、`STUB_HEAD_PREAMBLE`、`splitPrompts`（`## P<n>` 分段）、`test/helpers/controlled-provider.ts` 的 `openBalancedSession/seedToolHistory(prefixText)`、S03 plan、S04 `verbatimQuote(sourceKind:"any")`。

**Produces：**

W lane（4 case）：

- `review-fixtures/W-Q0x.json`：复制基底 Q 的 `seedText/witness/finalPrompt/oracle`，增加 `"lane":"W"`, `"base":"Q0x"`, `"requiresFold":true`, `"seedUsagePercent":45`, `"padSource":"repo-docs"`, `"warmup":{"files":2,"tokensEach":6000}`。
- `review-seed.mjs`：`buildReviewSeed` 读 `fx.seedUsagePercent`（默认沿用 65/30）；`padSource:"repo-docs"` 时 pad 正文来自 `docs/*.md` 按文件名排序拼接后的确定性切片（每段前加 `[dump-<i> offset=<n>]` 行），替代 `uniquePad`；导出 `warmupFiles(fx, repoRoot): Array<{name, text}>`（同一来源的另两段切片，各 ≈`tokensEach`，与 seed pad 不重叠）。
- `run-episode.mjs`：`spec.warmup` 存在时把 `warmupFiles` 写入 `<cwd>/notes/dump-a.txt`、`dump-b.txt`（在 git baseline 之前）。
- `cases/W-Q0x/TASK.md`：`## P1` 固定文本 `Before anything else run: cat notes/dump-a.txt then cat notes/dump-b.txt (two separate tool calls). Reply with one line per file.`；`## P2` 为基底 Q 的 TASK 正文（含 finalPrompt）。
- `cases.json`：4 条 W 条目（`runner:"review"`, `lane:"W"`, `windowProfile:"w64k"`, `arms:["native","balanced"]`, `reps:3`, `fixture` 同基底, `grader` 同基底, `editable` 追加 `notes/`, `seed: eval/local/seeds/review/W-Q0x.jsonl`, `reviewFixture`）。

X lane（1 case）：

- `log-workspace.mjs`：`setupLogWorkspace(cwd, { count, lines, marked, markLine, token, prefix="build" })` 生成 `logs/build-01..NN.log`，返回 `{ file, line, text }`；`markerLine(spec.logs)` 返回精确整行 `[ERROR] FIRST-ERROR-MARKER at build-07 line 210`。`run-episode.mjs` 的 H03 改用它（行为等价：12/2500/7/1700）。
- `cases.json` X01：`runner:"review"`, `lane:"X"`, `windowProfile:"w64k"`, `arms:["native","balanced"]`, `reps:3`, `seed:null`, `fixture:null`, `logs:{count:12, lines:300, marked:7, markLine:210, token:"FIRST-ERROR-MARKER"}`, `taskFile: eval/local/cases/X01/TASK.md`, `budget:"episodeLong"`, `grader:{kind:"file-oracle", trusted:[], editable:["marker.txt"], fileOracle:{file:"marker.txt", contains:"FIRST-ERROR-MARKER at build-07 line 210"}}`, `protectedPaths:["logs"]`, `evidence:{kind:"verbatim-quote", sourceKind:"any", linePattern:"FIRST-ERROR-MARKER at build-07 line 210"}`。
- `cases/X01/TASK.md`：`## P1`…`## P12` 各为 `Run: cat logs/build-<NN>.log and summarise in one line whether it shows a successful build.`（每次一份日志，一个工具调用）；`## P13`：`Which log contained the string FIRST-ERROR-MARKER and on which line? Quote that log line verbatim in your reply and write the same line to marker.txt.`
- `run-episode.mjs`：`spec.logs` 存在时调用 `setupLogWorkspace`，把 `markerLine` 写入 `<episode>/marker.json`；`spec.budget === "episodeLong"` 时使用 `REVIEW_BUDGET.episodeLong = { wallMs: 900_000, modelCalls: 40, toolCalls: 80 }`。

plan / gate / report：

- `scenarios.mjs`：`REGIME_LANES = { warm: ["W-Q01","W-Q03","W-Q05","W-Q07"], long: ["X01"] }`；`buildReviewPlan` 输出 `regimeLanes`、`expectedRegimePairs {warm:12, long:3}`、`exactQuoteIds:["Q05","X01"]`、`plannedEpisodes: 84`、`order[].lane`。
- `gate.mjs`：`regimePairsFromItt(plan, episodes, lane)`（与 `pairsFromItt` 同形，多 `nativeCompactions`）；`regimeSummary(pairs, planObjective): RegimeSummary`；X lane pair 的 `eligible = native.nativeCompactions ≥ 1`，不满足标 `unexercised`。
- `accounting.mjs`/`gate.mjs`：`evaluateTrial` 输入增加 `regimes`；任一 regime pair `criticalViolation` → `blocked`；其余不影响主决策，原样写入 `decision.regimes`。
- `report.mjs`：`candidates` 增加 `cold-aligned-fold`（Q `fresh-input ≤ −0.3` 且 warm `≥ −0.05`，两者 `known`）与 `post-compaction-evidence-delta`（X lane：native `quotedVerbatim===false && nativeCompactions≥1` 且同 rep candidate `quotedVerbatim===true`，≥2）；新增 "## regimes" 段。
- `review-matrix.json` 重新生成：84 episode 全 `UNRUN`。

## 非目标

不调折叠参数；不改 Q/C 的 seed 与 oracle；不把 W/X pair 合并进主分母；不实现 `cold-aligned-fold`；不跑真模型（S07）。

## TDD 第一条红测

新建 `test/host/warm-fold-regime.test.ts`（沿用 `test/host/after-fold-quality.test.ts` 的受控 Provider 用法）：

```ts
import { mkdtempSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { expect, it } from "vitest";
import { buildReviewSeed, warmupFiles } from "../../eval/local/review-seed.mjs";
import { loadReviewFixture } from "../../eval/local/scenarios.mjs";
import { loadOfficialPi } from "../helpers/official-pi.js";
import { openBalancedSession } from "../helpers/controlled-provider.js";

const folded = (c: unknown) => JSON.stringify(c).includes("pctx folded tool result");
const toolResults = (ms: Array<{ role: string; content: unknown }>) => ms.filter((m) => m.role === "toolResult");

it("W-Q01 does not fold on the first request and folds only after the warm-up dumps push usage past 60%", async () => {
  const fx = loadReviewFixture("W-Q01");
  const seed = buildReviewSeed(fx);
  expect(seed.targetTokens).toBe(Math.ceil(65_536 * 0.45));
  const resumeFile = join(mkdtempSync(join(tmpdir(), "wq01-")), "seed.jsonl");
  writeFileSync(resumeFile, seed.jsonl);
  const pi = await loadOfficialPi();
  // controlled script: tool calls return warmupFiles(fx)[i].text; reported usage climbs 47% → 57% → 67%
  const s = await openBalancedSession(pi, { contextWindow: 65_536, protectRecentBatches: 4, minRemovedTokens: 4096, resumeFile,
    script: warmupScript(warmupFiles(fx, repoRoot), [0.47, 0.57, 0.67]) });
  await s.session.prompt("cat notes/dump-a.txt");           // controlled tool returns warmupFiles(fx)[0].text
  expect(toolResults(s.captured[0]!.messages).some((m) => folded(m.content)), "first request must not fold at 45%").toBe(false);
  await s.session.prompt("cat notes/dump-b.txt");
  expect(toolResults(s.captured[1]!.messages).some((m) => folded(m.content)), "still below 60% before second dump lands").toBe(false);
  await s.session.prompt(fx.finalPrompt);
  const third = toolResults(s.captured[2]!.messages);
  expect(third.some((m) => folded(m.content)), "fold must happen mid-session once usage passed 60%").toBe(true);
  const dumps = third.filter((m) => JSON.stringify(m.content).includes("[dump-"));
  expect(dumps.length).toBeGreaterThan(0);
  expect(dumps.every((m) => !folded(m.content)), "the two warm-up dumps are inside the protected recent batches").toBe(true);
  await s.session.dispose?.();
}, 120_000);
```

追加到 `test/unit/review-scenarios.test.ts`：

```ts
import { markerLine, setupLogWorkspace } from "../../eval/local/log-workspace.mjs";

test("X01 workspace puts exactly one marker at build-07 line 210 and nothing else matches", () => {
  const dir = mkdtempSync(join(tmpdir(), "x01-"));
  const spec = { count: 12, lines: 300, marked: 7, markLine: 210, token: "FIRST-ERROR-MARKER" };
  const m = setupLogWorkspace(dir, spec);
  expect(m.file).toBe("logs/build-07.log");
  const all = readdirSync(join(dir, "logs")).sort();
  expect(all).toHaveLength(12);
  let hits = 0;
  for (const f of all) for (const [i, line] of readFileSync(join(dir, "logs", f), "utf8").split("\n").entries()) {
    if (line.includes(spec.token)) { hits++; expect(f).toBe("build-07.log"); expect(i + 1).toBe(210); expect(line).toBe(markerLine(spec)); }
  }
  expect(hits).toBe(1);
});

test("frozen review plan is 48 quality + 6 capability + 24 warm + 6 long UNRUN episodes", () => {
  const plan = buildReviewPlan();
  expect(plan.plannedEpisodes).toBe(84);
  expect(plan.expectedPairs).toBe(24);
  expect(plan.expectedRegimePairs).toEqual({ warm: 12, long: 3 });
  expect(plan.exactQuoteIds).toEqual(["Q05", "X01"]);
  expect(plan.order.every((o) => o.status === "UNRUN" && ["Q", "C", "W", "X"].includes(o.lane))).toBe(true);
});
```

运行：

```bash
pnpm exec vitest run test/host/warm-fold-regime.test.ts test/unit/review-scenarios.test.ts --config vitest.config.ts
```

`warmupScript(files, usagePercents)` 是测试内的小帮助函数，返回 `ControlledScript`（形状见 `test/helpers/controlled-provider.ts`）：第 i 次模型调用发出一个 `read` 工具调用、工具结果为 `files[i].text`、assistant usage 报告 `usagePercents[i] × contextWindow`；第 3 次直接给出文本回答。若现有 `ControlledScript`/`scriptUsagePercent` 不支持按请求变化 usage，扩展该 helper（允许越界，需在迭代记录说明）；不得改动 `src/`。`repoRoot = join(import.meta.dirname, "../..")`。

## 最小实施顺序

1. `log-workspace.mjs` + X01 单测；`run-episode.mjs` H03 改用它。
2. `review-seed.mjs`：`seedUsagePercent`、`padSource:"repo-docs"`、`warmupFiles`；W fixtures；`review-live-wiring.test.ts` "generated seeds hide the witness" 扩展到 W 与 `padSource` 断言（pad 中不含 witness、不含 `uniquePad` 形态 `^[a-z]+-pad[0-9a-z]+`）。
3. TASK.md ×5；`cases.json` +5；`local-cases.test.ts` 的 `runner==="review"` 期望列表加入 W/X。
4. `scenarios.mjs` plan v2；重新生成 `review-matrix.json`。
5. `run-matrix.mjs`：order 含 lane、X01 预算、`spec.logs`/`spec.warmup` 透传；`gate.mjs` regime 函数；`report.mjs` regimes 段与两个新候选。
6. 校准（Docker + 受控 Provider，不用真模型）：`node eval/local/run-matrix.mjs --mode controlled ...` 通过后，用一次 `--only X01 --arm native --dry` 检查 prompts 拆成 13 段；日志 token 校准留给 S07 首个 X01 native episode：若 `requests.jsonl` 显示单次 `cat` 的 fresh input < 4000 或工具结果含宿主截断标记，则把 `lines` 调到使单份 ≈ 5–7k tokens，重新冻结 `cases.json` 与 `review-matrix.json` 后**从头**开始 S07（校准 episode 作废，写入迭代记录）。

## 负例与边界

- W seed 的 witness 不得出现在 stub head（沿用 `STUB_HEAD_PREAMBLE`），也不得出现在 warm-up 文件里。
- `padSource:"repo-docs"` 切片不足以达到 `oldPad` token 时循环拼接并加 `[dump-i wrap=k]` 标记，不回退到 `uniquePad`。
- X01 `native` 若 `nativeCompactions === 0`（日志太短）→ 该 pair `unexercised`，regime 汇总 `eligible=false`，不进 `b/c`。
- X01 balanced 若 `folds === 0` → `foldRequired && !foldApplied` → 该 pair `unexercised`。
- 模型没有回读但从 native 摘要正确引用了整行 → 计成功（不奖励工具调用次数）。
- `marker.txt` 含正确行但引用不是整行（如缺 `[ERROR]` 前缀）→ `quotedVerbatim=false`，文件 oracle 仍可 `passed=true`；两者分别报告。

## 完成验证

```bash
pnpm exec vitest run test/unit/review-scenarios.test.ts test/unit/review-live-wiring.test.ts test/unit/local-cases.test.ts test/host/warm-fold-regime.test.ts --config vitest.config.ts
pnpm typecheck
node eval/local/run-matrix.mjs --mode controlled --config eval/local/review-matrix.json --out /tmp/pctx-s06-controlled
node eval/local/run-matrix.mjs --mode live --dry --config eval/local/review-matrix.json --out /tmp/pctx-s06-dry
```

`--dry` 输出 84 个 episode、含 `W-Q01/native/1`、`X01/balanced/3`。记录到 `docs/iterations/next-fixes.md` 的 `## S06` 段。

**验收：** 受控 Provider 下 W-Q01 首请求不折叠、越过 60% 后真实折叠且 warm-up dumps 受保护；X01 工作区与 marker 正确；plan 84 episode 全 UNRUN；regimes 与两个新候选进入 decision 结构。

**Review focus：** 确认 W lane 的第一请求 `resolveContextUsage` 读到的是 45% 而不是被 seed 里其他 usage 抬高；确认 X01 每个 `## P<n>` 只产生一次 `cat`，否则 batch 计数与 `protectRecentBatches` 的推演失效。
