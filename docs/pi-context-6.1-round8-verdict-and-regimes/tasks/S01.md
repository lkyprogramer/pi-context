# S01 · 登记见证偏离，持久化确认只对 derived-exposed 字段

依赖：无。实现前读取 [目标设计 §1](../design/00-target.md) 与 [AI 入口](../AI-START-HERE.md)。关闭 G09。

## 文件范围

- `src/plugin.ts`
- `test/unit/request-witness.test.ts`
- `test/host/balanced-wire.test.ts`
- `docs/iterations/next-fixes.md`

## 接口合同

**Consumes：** `buildActiveView` 产出的 `view.fields/view.branch`；`exposedEntryIds(view.branch)`（`src/projection/exposed.ts`）；`RequestWitnessTracker.confirmPersisted`（`44e8333b` 引入）。

**Produces：** `confirmPersistedFields(state, view, identity, exposed: ReadonlySet<string>)` 只对 `exposed.has(field.ref.entryId)` 的 text 字段调用 `confirmPersisted`；`applyContext` 中 `exposedEntryIds(view.branch)` 只计算一次，同时供 `confirmPersistedFields` 与 `planFold` 使用。行为不变：resume 首请求仍折叠（`test/host/balanced-wire.test.ts` "first request after a persisted seed must already fold" 与 `after-fold-quality.test.ts` "resume from a session file folds the first provider request" 必须继续通过）。

## 非目标

不恢复第 7 轮 R03 的"同进程 wire 见证前置"；不改 planner 过滤；不改折叠参数；不改 `exposedEntryIds` 语义；不动 `witness.ts`。

## TDD 第一条红测

追加到 `test/unit/request-witness.test.ts`（沿用该文件已有的 `user/call/result/done`、`parseConfig`、`createPlugin`、`applyContext` 导入）：

```ts
test("persisted confirmation skips fields without a later successful assistant", () => {
  const entries: NativeEntry[] = [user("u", null)];
  let parent = "u";
  const messages: AgentMessage[] = [{ role: "user", content: [{ type: "text", text: "question" }] }];
  for (let i = 1; i <= 5; i++) {
    entries.push(call(`c${i}`, parent, `call${i}`), result(`r${i}`, `c${i}`, `call${i}`, "z".repeat(8000)), done(`d${i}`, `r${i}`));
    parent = `d${i}`;
    messages.push(
      { role: "assistant", content: [{ type: "toolCall", id: `call${i}`, name: "read" }] },
      { role: "toolResult", toolCallId: `call${i}`, content: [{ type: "text", text: "z".repeat(8000) }] },
      { role: "assistant", content: [{ type: "text", text: "done" }] },
    );
  }
  // sixth batch: tool result present, no assistant after it → not derived-exposed
  entries.push(call("c6", parent, "call6"), result("r6", "c6", "call6", "z".repeat(8000)));
  parent = "r6";
  messages.push(
    { role: "assistant", content: [{ type: "toolCall", id: "call6", name: "read" }] },
    { role: "toolResult", toolCallId: "call6", content: [{ type: "text", text: "z".repeat(8000) }] },
  );
  const cfg = parseConfig({ schemaVersion: 6, profile: "balanced", storage: { mode: "memory-only" },
    fold: { protectRecentBatches: 1, minRemovedTokens: 8, minFoldableBytes: 1024 } });
  const state = createPlugin(cfg);
  const ctx = {
    cwd: process.cwd(),
    model: { id: "wire", provider: "controlled", contextWindow: 10000 },
    getContextUsage: () => ({ tokens: 8000, contextWindow: 10000, percent: 80 }),
    sessionManager: { getEntries: () => entries, getSessionId: () => "s", getLeafId: () => parent,
      getEntry: (id: string) => entries.find((e) => e.id === id) },
  };
  applyContext(state, structuredClone(messages), ctx as never);
  const id = state.lastIdentity!;
  const hashes = state.lastFieldHashes!;
  expect(state.witness.has(id, "r1:0", hashes.get("r1:0")!)).toBe(true);
  expect(state.witness.has(id, "r6:0", hashes.get("r6:0")!), "unexposed field must not be confirmed").toBe(false);
  state.index.closeSync();
});
```

运行：

```bash
pnpm exec vitest run test/unit/request-witness.test.ts --config vitest.config.ts
```

当前实现对 `r6:0` 返回 `true`，断言失败即有效红测。若 field key 格式不是 `${entryId}:${blockIndex}`，以 `state.lastFieldHashes` 的实际 key 为准调整测试，不改断言方向。

## 最小实施顺序

1. `applyContext`：在 `buildActiveView` 之后计算 `const exposed = view ? exposedEntryIds(view.branch) : new Set<string>()`；把 `exposed` 传给 `confirmPersistedFields` 与 `planFold`（替换现有 `exposed: exposedEntryIds(view.branch)`）。
2. `confirmPersistedFields` 增加 `exposed` 参数并过滤。
3. 运行 S01 定向测试 + `test/host/after-fold-quality.test.ts`，确认 resume 首请求折叠仍成立。
4. `docs/iterations/next-fixes.md` 追加 `## S01` 段，包含小节"R03 修订登记"：`44e8333b` 的内容、放弃同进程 wire 前置的理由、保留的三重保护（derived exposure / ActiveView 分支范围 / `compactionBoundary` 身份）、残余风险（模型切换后新模型只见 stub）。

## 负例与边界

- 分支上 `stopReason: "error"` 的 assistant 不构成 exposure（`exposedEntryIds` 已如此；加一条断言防回归）。
- 兄弟分支上的成功 assistant 不能让当前分支字段被确认（ActiveView 已按 `parentId` 链构建；断言 `lastFieldHashes` 不含兄弟分支 entry）。
- `identity` 为 null（scope 未建立）时不调用 `confirmPersisted`。
- 不得通过把 `protectRecentBatches` 置 0 或改 `minFoldableBytes` 让测试通过。

## 完成验证

```bash
pnpm exec vitest run test/unit/request-witness.test.ts test/host/balanced-wire.test.ts test/host/after-fold-quality.test.ts --config vitest.config.ts
pnpm typecheck
```

记录到 `docs/iterations/next-fixes.md` 的 `## S01` 段：RED/GREEN 命令与 exit、变更文件、R03 修订登记。

**验收：** 尚无后续成功 assistant 的持久化字段不被确认；resume 首请求折叠行为不变；偏离与理由已登记。

**Review focus：** 从 `applyContext` 追到 `planFold` 的 `exposed` 与 `witnessedView` 的 `has()`，确认两处使用同一个集合；不能只看新测试通过。
