# R04 · 把history分页修成真实多轮协议

依赖：R02。本任务实现前读取[目标设计](../design/00-target.md)及[执行协议](../AI-START-HERE.md)。

## 文件范围

- `src/history/page-snapshots.ts`
- `src/history/search.ts`
- `src/history/read.ts`
- `src/contracts.ts`
- `src/plugin.ts`
- `src/commands.ts`
- `test/unit/history-pagination.test.ts`
- `test/host/history-pagination.test.ts`

## 接口合同

**Consumes：** FieldRef/encodeRef/Scope、HistoryIndex；R02可信祖先链。

**Produces：** `SearchSnapshotStore`构造参数 `{maxSnapshots:16,maxHits:128,ttlMs:600000}`；searchHistory增加`snapshots`和`nowMs`依赖。HistoryResult统一使用nextCursor（cursor可在同次改造删除并更新调用方，不建兼容桥）。

上述新增函数/文件为本任务必须实现的API，不是当前仓库已经存在的能力。共用Fixture代码见[review-fixture.ts](../testing/review-fixture.ts)。

## 非目标

不重建旧PCR包，不更换宿主或模型，不调60%/40%/minRemoved/recent4默认参数，不加语义后台，不通过修改Oracle让失败变成功。未经安全预检不运行真Provider。

## TDD第一条红测

将以下测试写入文件清单中的对应test文件，合并而非覆盖已有测试：

```ts
import { expect, test } from "vitest";
 import { readHistory, formatHistoryResult } from "../../src/history/read.js";
 import { encodeRef, refForField, isFieldRef } from "../../src/history/refs.js";
 import { user } from "../helpers/context-audit-fixture.js";
 test("read cursor is bound to a specific field", () => {
   const entries=[user("a",null,"abcdefghij"),user("b","a","0123456789")];
   const scope={workspaceId:"w",sessionId:"s",leafId:"b",visibleEntryIds:new Set(["a","b"])};
   const a=refForField(scope,entries[0]!,0), b=refForField(scope,entries[1]!,0);
   if (!isFieldRef(a)||!isFieldRef(b)) throw new Error("invalid fixture");
   const getEntry=(id:string)=>entries.find(e=>e.id===id);
   const budget={maxBytes:4,maxTokens:100,estimateKind:"character-estimate" as const};
   const first=readHistory({scope,ref:encodeRef(a),budget,getEntry});
   expect(JSON.stringify(formatHistoryResult(first).content)).toContain(first.nextCursor!);
   const second=readHistory({scope,ref:encodeRef(b),cursor:first.nextCursor,budget,getEntry});
   expect(second.code).toBe("stale-cursor");
 });
```

运行：

```bash
pnpm exec vitest run test/unit/history-pagination.test.ts test/host/history-pagination.test.ts test/host/history-image.test.ts --config vitest.config.ts
```

新API未建立时的导入失败只是起始红测；实现最小API后必须确认断言在错误行为上失败，不能只保留“模块不存在”的红测记录。

## 最小实施顺序

1. 实现冻结有界SearchPageSnapshot，scope/query/config绑定和anchor ancestry校验。继续调用增加leaf属于正常推进，不能自动resetoffset。
2. 每页只读snapshot指定字段并重新authorize/hash；受预算部分返回时cursor指向实际遍历的下一个位置。过期/重启/branch divergence明确stale-cursor，不能假装第一页。
3. formatHistoryResult在模型content中打印nextCursor和scope/fidelity说明，read正文/图片保持独立blocks；budget计算包含metadata/ref，不仅excerpt。
4. readcursor检查encodedref完整一致以及sourceHash/kind，严格UTF8边界、next>current和EOF=null。保留已有中文/emoji/image正例。
5. 真实Pi受控模型连续调用search(page1)→search(page2)→read(page1)→read(page2)，只把上次tool result的content提供给模型；不得从details偷取cursor帮忙。

## 负例与边界

- 跨workspace/session/其他query/config、兄弟分支、TTL/restart都拒绝。
- 最小预算无法放一项时返回insufficient-context而非空ok。
- 128命中上限需标truncated，不能声称已遍历全部历史。

## 完成验证

```bash
pnpm exec vitest run test/unit/history-pagination.test.ts test/host/history-pagination.test.ts test/host/history-image.test.ts --config vitest.config.ts
pnpm typecheck
```

结果记录到 `docs/iterations/next-fixes.md` 的R04段：实现commit、运行命令/exit、修复反例、未执行环境。只提交本任务实际更改文件；不要提交Key、模型思考或整份私密会话。

**验收：** P04/P05/P06消失，多次真实工具调用能顺序完成，第一页不循环。

**Review focus：** 从默认扩展入口追到最终Provider/工具content或报告数值，不能只检查新helper自测。执行环境不足时标BLOCKED，断言不成立时标FAILED，两者不混用。
