# T03｜把Reducer真正交给模型，并保留完整观察块

**依赖：** T02  
**对应问题：** F01, F04  
**目标：** 模型首次看到工具输出时能知道结果；CAS中的原件没有在降噪前损坏。

## 文件边界

- `packages/contracts/src/types.ts`（现有；贯通真实数据合同）
- `packages/contracts/src/v2.ts`（现有；贯通真实数据合同）
- `packages/pi-adapter/src/message-codec.ts`（现有；贯通真实数据合同）
- `packages/runtime/src/observation-envelope.ts`（新建；受此任务控制）
- `packages/runtime/src/observation-service.ts`（现有）
- `packages/runtime/src/ports.ts`（现有）
- `packages/pi-adapter/src/tool-result-hook.ts`（现有）
- `apps/pi-context-runtime/src/composition-root.ts`（现有）
- `packages/runtime/test/observation-envelope.test.ts`（新建；受此任务控制）
- `tests/acceptance/tool-result-flow.test.ts`（现有）

另允许本任务的 `artifacts/lean-v4/tasks/T03.json` 和对应日志。不得新增无关包、第二套存储或云Provider依赖。接口触及未列文件时先记录必须修改的调用者，并扩展本任务allowedFiles；不悄悄扩大工作范围。

## 输入与输出

Consumes：T02可信Evidence来源和访问；现ProductionReducers。

Produces：encodeObservation/decodeObservation/renderObservationView；default composition返回正确visibleContent。

接口精确定义见 [CONTRACTS](../CONTRACTS.md)。目标新符号属于本任务实现，不假称当前仓库已有。测试helper来自T01真实宿主，不是固定回答的质量评估器。

## 第一个RED

在上述指定测试文件中加入以下具体回归（已有同名文件就在现有describe下增加）。遇到新目标模块不存在的编译失败，应继续实现接口后确认测试能够针对目标行为变红；不能只保存MODULE_NOT_FOUND就称行为缺陷已复现。

```ts
import { expect, it } from "vitest";
import { encodeObservation, decodeObservation, renderObservationView }
 from "../src/observation-envelope.js";
it("preserves an image and emits useful text instead of only a pointer", () => {
 const original = {format:"pcr-observation-v1" as const,toolCallId:"c",toolName:"read",
  content:[{type:"image",mimeType:"image/png",data:"c3ludGhldGlj"}],details:{},isError:false};
 expect(decodeObservation(encodeObservation(original))).toEqual(original);
 const text = {...original,toolName:"bash",isError:true,
  content:[{type:"text",text:"noise ".repeat(8000)+"FAILED UserServiceTest exit=1"}]};
 const view=renderObservationView({original:text,reducedText:"FAILED UserServiceTest exit=1",
  evidenceId:"ev_test",budgetTokens:500,estimate:t=>Math.ceil(t.length/4)});
 expect(JSON.stringify(view.content)).toContain("UserServiceTest");
 expect(JSON.stringify(view.content)).toContain("ev_test");
});
```

## 实施步骤

1. 先规范序列化完整content及必要details；从此raw指ObservationEnvelope，不再声称join(text)代表所有原件。对可序列化未知块保留类型。

2. 修tool-result-hook.asContent非text空文本路径；支持的block无损保存和传递，不支持降噪时bypass并记录原因。

3. 组合根返回reduced visibleContent，保留isError；短结果透传，长结果用关键文本+可实际读取Evidence ID。工具预算包含句柄/footer。

4. Exact reader区分raw envelope与文本页面；验证数据版本和hash；不把隐私原件原样发送模型。

5. 真实Pi test触发失败工具后断言下一次provider request含失败名称/exit，不只断言FTS保存了内容。

## 边界测试（均为必需）

- 短结果不可无谓替换pointer
- 两个text块边界可恢复
- image和空text的hash不同
- Reducer失败回原文或明确终止，不伪造新结果
- 写入失败无假句柄
- 检索工具结果不再次摄取

## 执行命令

```bash
pnpm exec vitest run packages/runtime/test/observation-envelope.test.ts
pnpm typecheck
```

命令使用目标仓库的已有pnpm环境；没有依赖时先安装冻结lockfile。T01创建的新统一命令仅供后续任务使用。实际模型命令只在T12显式授权后执行；其他任务用受控host或纯函数测试。

## 验收与失败处置

Tool Hook经真实宿主后下一请求含关键结果；image结构可恢复/未被静默丢；CAS/索引/可见文本的身份和hash各自明确。

修复后重跑同一RED与全部负例；保存命令、exit、失败原因和有效产物hash。任务可以因外部环境记blocked，但不得填passed。完成一个本地原子commit，不push。回归引入新失败时至多两轮定向修复，然后保存最小复现并继续其他ready任务。

**Reviewer重点：** 新结果是否来自真实产品调用点？是否仅把测试替身改成了期望答案？是否弱化权限/信息保留来满足性能？涉及Live的报告是否只更换了名称而没有观测相应能力？
