# T04｜禁止内容去重删除活动工具批次

**依赖：** T03  
**对应问题：** F03  
**目标：** 先保持原始动作序列，再压缩有证据的历史；不能靠少发消息获得伪Token收益。

## 文件边界

- `packages/core/src/materialization/dedup.ts`（现有）
- `packages/core/src/materialization/materializer.ts`（现有）
- `packages/pi-adapter/src/message-codec.ts`（现有）
- `packages/core/test/materialization/dedup.test.ts`（现有）
- `tests/acceptance/active-tool-batch.test.ts`（新建；受此任务控制）

另允许本任务的 `artifacts/lean-v4/tasks/T04.json` 和对应日志。不得新增无关包、第二套存储或云Provider依赖。接口触及未列文件时先记录必须修改的调用者，并扩展本任务allowedFiles；不悄悄扩大工作范围。

## 输入与输出

Consumes：当前HostMessage/opaque raw envelope；T03观察可见结果。

Produces：保留不同事件ID的dedup；raw tool-batch验证和受预算保护尾部。

接口精确定义见 [CONTRACTS](../CONTRACTS.md)。目标新符号属于本任务实现，不假称当前仓库已有。测试helper来自T01真实宿主，不是固定回答的质量评估器。

## 第一个RED

在上述指定测试文件中加入以下具体回归（已有同名文件就在现有describe下增加）。遇到新目标模块不存在的编译失败，应继续实现接口后确认测试能够针对目标行为变红；不能只保存MODULE_NOT_FOUND就称行为缺陷已复现。

```ts
import { expect, it } from "vitest";
import { dedupMaterializationMessages } from "../../src/materialization/dedup.js";
import type { HostMessage } from "@pcr/contracts";
it("retains separate active calls and identical results",()=>{
 const active:HostMessage[]=[
  {hostMessageId:"a1",role:"assistant",timestamp:1,sourceClass:"agent-derived",content:[]},
  {hostMessageId:"r1",role:"tool-result",toolCallId:"c1",timestamp:2,sourceClass:"untrusted-tool",content:[{type:"text",text:"OK"}]},
  {hostMessageId:"a2",role:"assistant",timestamp:3,sourceClass:"agent-derived",content:[]},
  {hostMessageId:"r2",role:"tool-result",toolCallId:"c2",timestamp:4,sourceClass:"untrusted-tool",content:[{type:"text",text:"OK"}]}];
 expect(dedupMaterializationMessages([],[],active).active.map(m=>m.hostMessageId))
  .toEqual(["a1","r1","a2","r2"]);
});
```

## 实施步骤

1. 只对同一hostMessageId重复投影去重；活动content相同不是重复事件。

2. 保留opaque块和callIDs，在raw层构建batch；不要给纯toolCall补一个近似text再继续语义去重。

3. 历史裁剪只移动完整已消费批次；当前未消费结果永远保持原协议顺序。

4. 从Pi构造两个不同调用同输出，经过context后验证全部ID/顺序和下一模型看到的结果。

## 边界测试（均为必需）

- 同ID重复注入可去重
- 同文不同时间用户请求不合并
- 图片/纯toolcall不同ID不合并
- missing/orphan拒绝而非“修复”为删除动作
- 预算不足不能悄悄删未消费batch

## 执行命令

```bash
pnpm exec vitest run packages/core/test/materialization/dedup.test.ts
pnpm typecheck
```

命令使用目标仓库的已有pnpm环境；没有依赖时先安装冻结lockfile。T01创建的新统一命令仅供后续任务使用。实际模型命令只在T12显式授权后执行；其他任务用受控host或纯函数测试。

## 验收与失败处置

原隔离五条变三条反例不再出现；完整Pi实际调用两次且两次结果进入下一视图；token收益不得来自活动事件丢失。

修复后重跑同一RED与全部负例；保存命令、exit、失败原因和有效产物hash。任务可以因外部环境记blocked，但不得填passed。完成一个本地原子commit，不push。回归引入新失败时至多两轮定向修复，然后保存最小复现并继续其他ready任务。

**Reviewer重点：** 新结果是否来自真实产品调用点？是否仅把测试替身改成了期望答案？是否弱化权限/信息保留来满足性能？涉及Live的报告是否只更换了名称而没有观测相应能力？
