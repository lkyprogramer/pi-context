# T01｜恢复正确认证fixture，并收敛检查入口

**依赖：** 无  
**对应问题：** F14, F15  
**目标：** 保持已修复的strict/pack；将旧未认证fixture修成一正一负，不放宽生产认证规则。提供fast/local两个无Provider入口。

## 文件边界

- `tests/tasks/t31.test.ts`（现有）
- `tests/helpers/product-harness.ts`（新建；受此任务控制）
- `tests/acceptance/controlled-host-compaction.test.ts`（现有）
- `package.json`（现有）
- `.github/workflows/required.yml`（现有）
- `.github/workflows/compatibility.yml`（现有）
- `docs/COMPATIBILITY.md`（现有）

另允许本任务的 `artifacts/lean-v4/tasks/T01.json` 和对应日志。不得新增无关包、第二套存储或云Provider依赖。接口触及未列文件时先记录必须修改的调用者，并扩展本任务allowedFiles；不悄悄扩大工作范围。

## 输入与输出

Consumes：现有patched Pi0.84.4、user-input-flow与controlled-host-compaction真实宿主测试。

Produces：CONTRACTS中的createProductHarness；pnpm check:fast/check:local；与当前支持版本一致的文档。

接口精确定义见 [CONTRACTS](../CONTRACTS.md)。目标新符号属于本任务实现，不假称当前仓库已有。测试helper来自T01真实宿主，不是固定回答的质量评估器。

## 第一个RED

在上述指定测试文件中加入以下具体回归（已有同名文件就在现有describe下增加）。遇到新目标模块不存在的编译失败，应继续实现接口后确认测试能够针对目标行为变红；不能只保存MODULE_NOT_FOUND就称行为缺陷已复现。

```ts
import { expect, it } from "vitest";
import { createProductHarness } from "../helpers/product-harness.js";
it("persists authenticated intent through the real input path", async () => {
  const h = await createProductHarness();
  try {
    await h.prompt("Do not deploy production. 改为 version 7。");
    for (let i = 0; i < 5; i++) {
      await h.prompt(`Work log ${i}; current requested version remains 7. ${"Local build observation. ".repeat(180)}`);
    }
    await h.compact();
    const entries = h.rawEntries() as Array<{type?: string; summary?: string}>;
    expect(entries.filter(e => e.type === "compaction").at(-1)?.summary)
      .toMatch(/version\s*=?\s*7/);
  } finally { await h.close(); }
});
```

## 实施步骤

1. 从controlled-host-compaction提取真实helper；继续由session.prompt经过input/message_start产生linked receipt。不要自己拼metadata以跳过认证逻辑。

2. helper固定采用受控Provider，不联网；grow只为保证有可压前缀，真实前缀为空应是独立no-op测试。t31原raw append改为明确无回执负例：应Native fallback而不是expect PCR。另用真实helper保留拒绝被改写成must-not/硬编码heads的正例。

3. check:fast = boundaries+typecheck+unit；check:local按现有互斥lane执行contract/integration/acceptance/packed。删除云Provider启动路径但不删除组件测试。

4. 治理检查单列advisory且报告真实未保护；默认fast不能因GH_TOKEN缺失调用GitHub。min/current相同版本不重复扩大矩阵。

## 边界测试（均为必需）

- 缺receipt不创建authenticated Directive
- 助手/工具文本不能冒充用户
- helper shutdown/restart无真实HOME读取
- strict/packed原成功继续保持

## 执行命令

```bash
pnpm exec vitest run tests/tasks/t31.test.ts
pnpm typecheck
pnpm test:packed
```

命令使用目标仓库的已有pnpm环境；没有依赖时先安装冻结lockfile。T01创建的新统一命令仅供后续任务使用。实际模型命令只在T12显式授权后执行；其他任务用受控host或纯函数测试。

## 验收与失败处置

t31与新增真实正负例均绿；当前节点的旧type错误不重现；快检不联网，不触发Provider；支持范围不写stock兼容。

修复后重跑同一RED与全部负例；保存命令、exit、失败原因和有效产物hash。任务可以因外部环境记blocked，但不得填passed。完成一个本地原子commit，不push。回归引入新失败时至多两轮定向修复，然后保存最小复现并继续其他ready任务。

**Reviewer重点：** 新结果是否来自真实产品调用点？是否仅把测试替身改成了期望答案？是否弱化权限/信息保留来满足性能？涉及Live的报告是否只更换了名称而没有观测相应能力？
