# Compaction Provider

Custom result 使用 Pi `preparation.firstKeptEntryId/tokensBefore`；summary 渲染 directive/continuity/heads/pointers；details 存 schema/generation hashes。Overflow 只用 deterministic candidate。
