# A19 — 构建 Snapshot-scoped Evidence Pointer Catalog

## 元数据

- Wave：`W2`
- Depends on：A10, A11, A12
- Findings：F111
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

替代 global lastPointers。

## 允许修改

- `packages/runtime/src/evidence/**`
- `packages/storage-node/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. catalog by cursor/source span
2. active status/TTL/authority
3. checkpoint selects task-relevant pointers
4. stable short refs

## 必须先写的 RED 测试

- concurrent tool results
- branch split
- old pointer retired/new active

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/runtime/test/evidence-catalog.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

每个 checkpoint pointer 可追溯到本 snapshot source span。

## Evidence

```json
{
  "taskId": "A19",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A19/red.log",
  "greenLog": "artifacts/task-evidence/A19/green.log",
  "fullGateLog": "artifacts/task-evidence/A19/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F111"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a19): 构建 Snapshot-scoped Evidence Pointer Catalog"
```
