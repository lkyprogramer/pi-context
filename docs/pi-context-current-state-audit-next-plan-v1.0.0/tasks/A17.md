# A17 — 修复多语言 Temporal Assignment Parser

## 元数据

- Wave：`W2`
- Depends on：A11
- Findings：F121
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

正确解析 `instead use version 7`、`set version to 7`、中文/符号赋值。

## 允许修改

- `packages/core/src/directives/temporal.ts`
- `packages/core/test/directives/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. grammar/ordered patterns
2. 保留 exact quote
3. ambiguous clause only exact no invented KV
4. typed normalizer

## 必须先写的 RED 测试

- English/Chinese/Unicode
- key collision
- negative/decimal/path values

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/core/test/directives/temporal*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

English live checkpoint claim 必须 key=version,value=7。

## Evidence

```json
{
  "taskId": "A17",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A17/red.log",
  "greenLog": "artifacts/task-evidence/A17/green.log",
  "fullGateLog": "artifacts/task-evidence/A17/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F121"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a17): 修复多语言 Temporal Assignment Parser"
```
