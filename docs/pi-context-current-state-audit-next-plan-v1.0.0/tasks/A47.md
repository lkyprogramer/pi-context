# A47 — 运行 Performance/Cache/Metadata Ablation

## 元数据

- Wave：`W5`
- Depends on：A27, A31, A42
- Findings：F113, F138
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

定位 temporal/branch input inflation。

## 允许修改

- `artifacts/runs/performance-cache/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. payload token decomposition
2. cache hit/miss
3. metadata on/off
4. B1/B2 materializer sections
5. p50/p95

## 必须先写的 RED 测试

- warm/cold cache
- CJK/English
- tool schema sizes

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:performance-cache
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

给出可解释的 section/cost waterfall，选定 checkpoint layout。

## Evidence

```json
{
  "taskId": "A47",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A47/red.log",
  "greenLog": "artifacts/task-evidence/A47/green.log",
  "fullGateLog": "artifacts/task-evidence/A47/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F113", "F138"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a47): 运行 Performance/Cache/Metadata Ablation"
```
