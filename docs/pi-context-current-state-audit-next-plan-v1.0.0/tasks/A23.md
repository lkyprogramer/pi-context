# A23 — 多 Workspace/Session/Branch 隔离 Gate

## 元数据

- Wave：`W2`
- Depends on：A10, A16, A19
- Findings：F111, F112, F133, F140
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

并发 scope fuzz 与跨 scope read/write/recall。

## 允许修改

- `tests/security/**`
- `tests/acceptance/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. matrix workspace/session/leaf/model
2. random interleaving
3. close/reopen
4. branch ancestry

## 必须先写的 RED 测试

- cross-read
- pointer injection
- last-owner ambiguity

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm test:security -- scope
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

0 cross-scope accepted，所有 denied 有稳定错误码。

## Evidence

```json
{
  "taskId": "A23",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A23/red.log",
  "greenLog": "artifacts/task-evidence/A23/green.log",
  "fullGateLog": "artifacts/task-evidence/A23/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F111", "F112", "F133", "F140"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a23): 多 Workspace/Session/Branch 隔离 Gate"
```
