# A41 — 不可变 Full Run Bundle 与复核工具

## 元数据

- Wave：`W4`
- Depends on：A34, A38, A40
- Findings：F129, F130
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

保留完整 JSONL/response/payload/usage/environment，不删临时证据。

## 允许修改

- `packages/benchmark/src/report/**`
- `schemas/run*.json`
- `scripts/benchmark/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. content-addressed bundle
2. redaction manifest
3. provenance commit/diff/container/model/config
4. score replay
5. public/private split

## 必须先写的 RED 测试

- tamper detection
- absolute path rejection
- re-score two-run

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:bundle:verify <bundle>
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

第三方可不调用模型重算全部 metrics/decision。

## Evidence

```json
{
  "taskId": "A41",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A41/red.log",
  "greenLog": "artifacts/task-evidence/A41/green.log",
  "fullGateLog": "artifacts/task-evidence/A41/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F129", "F130"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a41): 不可变 Full Run Bundle 与复核工具"
```
