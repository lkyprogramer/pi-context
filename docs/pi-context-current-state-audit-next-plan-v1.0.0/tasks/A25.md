# A25 — 统一 Capacity/Cache/Cost Usage Telemetry

## 元数据

- Wave：`W3`
- Depends on：A13, A14, A24
- Findings：F107, F108, F116, F139, F142
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

保存完整 usage 与 payload count。

## 允许修改

- `packages/runtime/src/telemetry/**`
- `packages/benchmark/src/**`
- `packages/pi-adapter/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. input/cacheRead/cacheWrite/output/total
2. capacity tokens独立
3. pricing table/version
4. reconcile provider vs estimator

## 必须先写的 RED 测试

- cache hit/miss
- provider missing fields
- retry/overflow

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/runtime/test/usage*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

报告不再把 usage.input 命名为 full input。

## Evidence

```json
{
  "taskId": "A25",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A25/red.log",
  "greenLog": "artifacts/task-evidence/A25/green.log",
  "fullGateLog": "artifacts/task-evidence/A25/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F107", "F108", "F116", "F139", "F142"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a25): 统一 Capacity/Cache/Cost Usage Telemetry"
```
