# A35 — Probe-only Scorer 与 Normalizer

## 元数据

- Wave：`W4`
- Depends on：A32, A34
- Findings：F114
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

Summary不参与 executor答案评分。

## 允许修改

- `packages/benchmark/src/scoring/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. version/yes-no/path/error normalizers
2. non-answer/tool-call detection
3. family rubrics
4. deterministic tests

## 必须先写的 RED 测试

- tu-00/01/08/09/10/11 regressions
- affirmative deploy
- yes merge

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/benchmark/test/scoring/probe*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

旧 report temporal重放得到 B0 0/6、B1 5/6。

## Evidence

```json
{
  "taskId": "A35",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A35/red.log",
  "greenLog": "artifacts/task-evidence/A35/green.log",
  "fullGateLog": "artifacts/task-evidence/A35/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F114"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a35): Probe-only Scorer 与 Normalizer"
```
