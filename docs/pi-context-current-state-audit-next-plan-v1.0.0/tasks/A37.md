# A37 — Tools-enabled Environment Closed-loop

## 元数据

- Wave：`W4`
- Depends on：A34, A35
- Findings：F114, F120
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

模型自己选择工具，环境断言评分。

## 允许修改

- `packages/benchmark/src/continuation/**`
- `tests/live-gate/workspaces/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. workspace clone per arm
2. hidden task
3. tool call capture
4. git/test/side-effect assertions
5. timeout/cancel

## 必须先写的 RED 测试

- public API prohibition
- version update
- test repair
- forbidden deploy

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:closed-loop:smoke
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Executor不含预编程正确 tool call；结果只由环境判定。

## Evidence

```json
{
  "taskId": "A37",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A37/red.log",
  "greenLog": "artifacts/task-evidence/A37/green.log",
  "fullGateLog": "artifacts/task-evidence/A37/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F114", "F120"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a37): Tools-enabled Environment Closed-loop"
```
