# A40 — Cluster Statistics 与 Normative Gate v3

## 元数据

- Wave：`W4`
- Depends on：A32, A35, A37, A39
- Findings：F124, F128, F135, F136
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

实现样本规模、per-family、CI、最小改善。

## 允许修改

- `packages/benchmark/src/statistics/**`
- `packages/benchmark/src/report/engine.ts`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. cluster bootstrap
2. McNemar
3. NI margin
4. sample profile validation
5. hard→quality→efficiency层级

## 必须先写的 RED 测试

- tiny positive net not adopt
- 5 clusters rejected
- family regression blocks

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/benchmark/test/gate-v3.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Gate 不可由 30×1 或 realizedNet=0.1 adopt。

## Evidence

```json
{
  "taskId": "A40",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A40/red.log",
  "greenLog": "artifacts/task-evidence/A40/green.log",
  "fullGateLog": "artifacts/task-evidence/A40/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F124", "F128", "F135", "F136"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a40): Cluster Statistics 与 Normative Gate v3"
```
