# A46 — 运行 Fault/Security Matrix

## 元数据

- Wave：`W5`
- Depends on：A16, A23, A38
- Findings：F104, F126, F137
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

存储/恢复/scope/secret/injection发布门。

## 允许修改

- `artifacts/runs/fault-security/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. process kill phases
2. blob corruption/key rotation
3. cross-scope
4. injection variants
5. bundle leak scan

## 必须先写的 RED 测试

- Linux/macOS matrix
- fuzz seeds retained

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:fault-security
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Critical/High=0，crash replay=100%。

## Evidence

```json
{
  "taskId": "A46",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A46/red.log",
  "greenLog": "artifacts/task-evidence/A46/green.log",
  "fullGateLog": "artifacts/task-evidence/A46/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F104", "F126", "F137"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a46): 运行 Fault/Security Matrix"
```
