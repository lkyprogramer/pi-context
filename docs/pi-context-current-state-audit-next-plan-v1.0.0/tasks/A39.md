# A39 — 完整 Realized Net 与 Pair-preserving Cost

## 元数据

- Wave：`W4`
- Depends on：A25, A34, A37
- Findings：F116, F127, F128, F142
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

全成本、配对成功、失败惩罚。

## 允许修改

- `packages/benchmark/src/economics/**`
- `packages/benchmark/src/report/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. serialized capacity
2. cache pricing
3. summary/recall/tool cost
4. latency
5. both-success pair set
6. failure penalty sensitivity

## 必须先写的 RED 测试

- different success sets
- missing usage
- cache price changes

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:economics:verify
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

报告同时给 capacity/cost/net，任何 median都保留 case pairing。

## Evidence

```json
{
  "taskId": "A39",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A39/red.log",
  "greenLog": "artifacts/task-evidence/A39/green.log",
  "fullGateLog": "artifacts/task-evidence/A39/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F116", "F127", "F128", "F142"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a39): 完整 Realized Net 与 Pair-preserving Cost"
```
