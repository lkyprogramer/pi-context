# A03 — 修复跨进程 CAS/Saga 测试 Runner

## 元数据

- Wave：`W0`
- Depends on：A00
- Findings：F101, F104
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

不依赖未声明 jiti；使用编译后的 JS helper 或 `node --import` 明确依赖。

## 允许修改

- `packages/storage-node/test/**`
- `packages/testkit/**`
- `package.json`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 新增 process-fixture builder
2. 等待 ready file/IPC 而非固定 sleep
3. 信号断言允许平台等价 exit code 并验证实际 crash point

## 必须先写的 RED 测试

- two writers existing-object
- kill before/after fsync
- orphan recovery replay

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/storage-node/test/blob-store.test.ts packages/storage-node/test/saga-store.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

两平台无 unhandled rejection、无 5s 偶然超时。

## Evidence

```json
{
  "taskId": "A03",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A03/red.log",
  "greenLog": "artifacts/task-evidence/A03/green.log",
  "fullGateLog": "artifacts/task-evidence/A03/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F101", "F104"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a03): 修复跨进程 CAS/Saga 测试 Runner"
```
