# A20 — 实现 Tool Outcome Attestation 与 Claim Authority

## 元数据

- Wave：`W2`
- Depends on：A11, A19
- Findings：无直接 Finding；属于 Gate/集成任务
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

tests passed/deployed/deleted 等只能由对应成功 tool evidence升级。

## 允许修改

- `packages/core/src/security/**`
- `packages/runtime/src/claims/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. tool taxonomy
2. claim transition rules
3. assistant claims remain propose/inform
4. contradiction handling

## 必须先写的 RED 测试

- assistant says passed but tool fails
- read log contains passed
- deploy forbidden

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/core/test/security/outcome*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

unsupported high-risk outcome=0 在 mutation tests 中成立。

## Evidence

```json
{
  "taskId": "A20",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A20/red.log",
  "greenLog": "artifacts/task-evidence/A20/green.log",
  "fullGateLog": "artifacts/task-evidence/A20/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": []
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a20): 实现 Tool Outcome Attestation 与 Claim Authority"
```
