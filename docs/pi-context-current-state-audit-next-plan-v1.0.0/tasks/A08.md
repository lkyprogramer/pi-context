# A08 — 让默认 Extension 只使用 ProductionRuntimeCompositionRoot

## 元数据

- Wave：`W1`
- Depends on：A04, A07
- Findings：F105
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

删除 default `bindClaimedRuntime` 中 ad-hoc 子系统 new；所有 Hook 路由到 root/session。

## 允许修改

- `apps/pi-context-runtime/src/extension.ts`
- `apps/pi-context-runtime/src/composition-root.ts`
- `tests/acceptance/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. Production root 注册全部 hooks
2. 公开 factory 与 packed entry 使用相同 root
3. fixture factory 移入 testkit

## 必须先写的 RED 测试

- one owner only
- no direct materializer/compactor in adapter
- packed root path

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/acceptance/product-runtime-path.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

静态扫描证明 adapter 只调用 RuntimeSession methods。

## Evidence

```json
{
  "taskId": "A08",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A08/red.log",
  "greenLog": "artifacts/task-evidence/A08/green.log",
  "fullGateLog": "artifacts/task-evidence/A08/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F105"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a08): 让默认 Extension 只使用 ProductionRuntimeCompositionRoot"
```
