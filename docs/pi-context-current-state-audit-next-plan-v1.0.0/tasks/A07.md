# A07 — 重开并重构 Finding Closure Gate

## 元数据

- Wave：`W0`
- Depends on：A00, A04
- Findings：F134, F141
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

Finding 关闭必须绑定 current commit 的实现测试、verification test、required CI。

## 允许修改

- `docs/**/findings/**`
- `scripts/findingctl.py`
- `scripts/gates/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 增加 `verifiedAtCommit`/`verificationRunId`/`verificationTask`
2. CI red 自动 reopen release-blocking finding
3. 禁止仅列文件路径作为 closure evidence

## 必须先写的 RED 测试

- stale commit closure rejected
- missing verification rejected
- CI red blocks P0 close

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
python3 scripts/findingctl.py verify-all
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

本包 F101–F142 与旧 F001–F038 有一一映射，未满足证据保持 open。

## Evidence

```json
{
  "taskId": "A07",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A07/red.log",
  "greenLog": "artifacts/task-evidence/A07/green.log",
  "fullGateLog": "artifacts/task-evidence/A07/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F134", "F141"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a07): 重开并重构 Finding Closure Gate"
```
