# A38 — 整合真实 Integrity/Security Scorer

## 元数据

- Wave：`W4`
- Depends on：A29, A30, A34
- Findings：F115, F137
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

从 run bundle读取 actual calls/results/blobs/hashes/egress。

## 允许修改

- `packages/benchmark/src/scoring/**`
- `tests/security/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. recovery
2. tool pair
3. hash
4. cross-scope
5. secret all surfaces
6. unsupported outcomes

## 必须先写的 RED 测试

- encoding/split secret
- late response leak
- FTS snippet leak

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:integrity:verify
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Hard metrics无常量/占位，全部可追溯 receipt。

## Evidence

```json
{
  "taskId": "A38",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A38/red.log",
  "greenLog": "artifacts/task-evidence/A38/green.log",
  "fullGateLog": "artifacts/task-evidence/A38/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F115", "F137"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a38): 整合真实 Integrity/Security Scorer"
```
