# A15 — 删除 unbound Scope 降级

## 元数据

- Wave：`W1`
- Depends on：A08, A10
- Findings：F112
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

身份缺失时 typed hard/soft failure，不创建持久 unbound state。

## 允许修改

- `apps/pi-context-runtime/src/extension.ts`
- `packages/pi-adapter/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. context soft fallback raw messages
2. compaction Native fallback
3. storage/retrieval fail closed
4. diagnostic无敏感路径

## 必须先写的 RED 测试

- missing session manager
- missing lineage/model
- malformed ctx

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/acceptance/scope-failure.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

不存在 `sessionId:"unbound"`/zero cursor 的持久写入。

## Evidence

```json
{
  "taskId": "A15",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A15/red.log",
  "greenLog": "artifacts/task-evidence/A15/green.log",
  "fullGateLog": "artifacts/task-evidence/A15/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F112"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a15): 删除 unbound Scope 降级"
```
