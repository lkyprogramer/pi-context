# A06 — 统一 Release Metadata 与入口策略

## 元数据

- Wave：`W0`
- Depends on：A02, A04
- Findings：F131, F132
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

裁决 internal tarball 或 public npm；许可证、private、exports、dist 与文档一致。

## 允许修改

- `apps/pi-context-runtime/package.json`
- `apps/pi-context-runtime/dist/**`
- `scripts/release/**`
- `docs/INSTALL.md`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 若 internal：明确 `private:true` 与禁止 npm publish；若 public：SPDX + private:false
2. 仓库 dist 改为真正编译输出或删除 direct `pi -e` 文档
3. tarball 与 repo dev entry 用同一 build graph

## 必须先写的 RED 测试

- manifest policy test
- dist cannot import .ts outside package
- tarball typecheck

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm build
pnpm test:packed
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

不存在 UNLICENSED/public install 冲突，dev/release behavior hash 可对比。

## Evidence

```json
{
  "taskId": "A06",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A06/red.log",
  "greenLog": "artifacts/task-evidence/A06/green.log",
  "fullGateLog": "artifacts/task-evidence/A06/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F131", "F132"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a06): 统一 Release Metadata 与入口策略"
```
