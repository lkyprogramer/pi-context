# A24 — Envelope-aware Serialized Payload Token Pricing

## 元数据

- Wave：`W3`
- Depends on：A14
- Findings：F107, F139
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

按最终 Pi/provider payload计价 raw envelope字段。

## 允许修改

- `packages/pi-adapter/src/message-codec.ts`
- `packages/core/src/budget/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. canonical serializer
2. compactionSummary.summary
3. assistant toolCall/thinking
4. toolResult metadata/image/custom
5. route tokenizer fallback

## 必须先写的 RED 测试

- round-trip price equality
- opaque roles
- huge active turn

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/core/test/budget/envelope-pricing.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

estimate 与 payload count误差 P95≤预注册阈值，hard active turn不漏算。

## Evidence

```json
{
  "taskId": "A24",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A24/red.log",
  "greenLog": "artifacts/task-evidence/A24/green.log",
  "fullGateLog": "artifacts/task-evidence/A24/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F107", "F139"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a24): Envelope-aware Serialized Payload Token Pricing"
```
