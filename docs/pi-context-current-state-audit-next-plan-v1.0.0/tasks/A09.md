# A09 — 补全 RuntimeSession Ports 与状态机

## 元数据

- Wave：`W1`
- Depends on：A08
- Findings：F105, F133, F141
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

RuntimeSession 统一 ingress/materialize/compact/retrieve/recover/receipt。

## 允许修改

- `packages/runtime/src/runtime-session.ts`
- `packages/runtime/src/ports.ts`
- `packages/runtime/test/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 定义 scope-complete ports
2. 同 session 串行 mutation，read snapshot可并发
3. close 后所有方法 typed fail

## 必须先写的 RED 测试

- state transitions
- abort propagation
- close race

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/runtime/test/runtime-session*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Context/Compaction 不再绕过 RuntimeSession。

## Evidence

```json
{
  "taskId": "A09",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A09/red.log",
  "greenLog": "artifacts/task-evidence/A09/green.log",
  "fullGateLog": "artifacts/task-evidence/A09/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F105", "F133", "F141"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a09): 补全 RuntimeSession Ports 与状态机"
```
