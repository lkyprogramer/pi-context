# A13 — 持久化 View 与 Cache Receipt

## 元数据

- Wave：`W1`
- Depends on：A12
- Findings：F108
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

receipt 持久化而不是每次 open 内存数组。

## 允许修改

- `packages/storage-node/src/**`
- `packages/core/src/materialization/**`
- `packages/runtime/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. receipt schema/table
2. first-different/section hashes
3. provider usage reconciliation
4. retention/GC

## 必须先写的 RED 测试

- restart head
- cache on/off outputHash same
- receipt cross-scope denied

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/core/test/materialization/cache*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

连续请求可计算真实稳定前缀与 cache rewrite。

## Evidence

```json
{
  "taskId": "A13",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A13/red.log",
  "greenLog": "artifacts/task-evidence/A13/green.log",
  "fullGateLog": "artifacts/task-evidence/A13/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F108"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a13): 持久化 View 与 Cache Receipt"
```
