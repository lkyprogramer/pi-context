# A30 — Tool-pair/Retained-tail/Two-run Hash Verifier

## 元数据

- Wave：`W3`
- Depends on：A24, A28
- Findings：F110
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

从真实 Pi messages 计算，不写死。

## 允许修改

- `packages/core/src/compaction/**`
- `packages/pi-adapter/src/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. parse toolCall/result
2. atomic cut
3. retained tail exact IDs
4. render twice fresh process

## 必须先写的 RED 测试

- orphan result/call
- split turn
- nondeterministic ordering

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/integration/checkpoint-hard-gates.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

hard metrics来自 verifier receipt，0/1不能由 runner常量填充。

## Evidence

```json
{
  "taskId": "A30",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A30/red.log",
  "greenLog": "artifacts/task-evidence/A30/green.log",
  "fullGateLog": "artifacts/task-evidence/A30/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F110"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a30): Tool-pair/Retained-tail/Two-run Hash Verifier"
```
