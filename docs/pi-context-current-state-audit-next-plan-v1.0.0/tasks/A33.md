# A33 — 生成真实 A1-shaped 冻结 Pi Trace

## 元数据

- Wave：`W4`
- Depends on：A21, A32
- Findings：F117
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

通过真实 Pi Extension tool_result ingress生成 JSONL+Runtime store snapshot。

## 允许修改

- `packages/benchmark/src/trace/**`
- `tests/live-gate/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. run A1 once then freeze
2. source/cut/retained tail manifest
3. same snapshot copied per arm
4. raw secrets encrypted/redacted bundle

## 必须先写的 RED 测试

- hash equality
- copy isolation
- host ack complete

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:trace:freeze
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

B0/B1/B2 输入 hash完全相等且 ingress已真实执行。

## Evidence

```json
{
  "taskId": "A33",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A33/red.log",
  "greenLog": "artifacts/task-evidence/A33/green.log",
  "fullGateLog": "artifacts/task-evidence/A33/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F117"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a33): 生成真实 A1-shaped 冻结 Pi Trace"
```
