# A11 — 落地持久 Directive/Claim/Continuity Store

## 元数据

- Wave：`W1`
- Depends on：A09
- Findings：F106, F109
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

Context 与 Compaction 从同一 stores 读取 active state。

## 允许修改

- `packages/storage-node/src/**`
- `packages/runtime/src/**`
- `packages/core/src/directives/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. SQLite schema/ports
2. supersession transaction
3. continuity revisions/task fronts/errors/side effects
4. claim outcome authority

## 必须先写的 RED 测试

- restart persists active state
- superseded excluded
- cross-scope denied

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/runtime/test/state-store*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

重启后 active heads 与重启前 hash 一致。

## Evidence

```json
{
  "taskId": "A11",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A11/red.log",
  "greenLog": "artifacts/task-evidence/A11/green.log",
  "fullGateLog": "artifacts/task-evidence/A11/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F106", "F109"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a11): 落地持久 Directive/Claim/Continuity Store"
```
