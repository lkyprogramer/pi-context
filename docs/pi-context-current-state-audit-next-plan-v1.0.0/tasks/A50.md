# A50 — Semantic/Background Beta 独立 Gate

## 元数据

- Wave：`W6`
- Depends on：A48
- Findings：F113
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

仅 deterministic adopt/keep-reducers 后按数据决定。

## 允许修改

- `packages/runtime/src/semantic/**`
- `packages/worker/**`
- `artifacts/gates/semantic-v3/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. candidate snapshot完整身份
2. durable state/fencing
3. verifier mutation
4. B2 closed-loop ablation
5. wasted work rate

## 必须先写的 RED 测试

- stale model/branch/config
- verifier bypass mutation

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm gate:semantic-v3
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

默认仍 off；只有 B2 质量/净值显著正才允许 beta。

## Evidence

```json
{
  "taskId": "A50",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A50/red.log",
  "greenLog": "artifacts/task-evidence/A50/green.log",
  "fullGateLog": "artifacts/task-evidence/A50/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F113"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a50): Semantic/Background Beta 独立 Gate"
```
