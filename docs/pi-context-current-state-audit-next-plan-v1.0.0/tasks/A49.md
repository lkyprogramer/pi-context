# A49 — 完成 Hermetic Package/Release Matrix

## 元数据

- Wave：`W5`
- Depends on：A05, A06, A48
- Findings：F103, F132
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

当前 commit tarball在空 HOME、Linux/macOS、Node/Pi matrix验证。

## 允许修改

- `artifacts/release/**`
- `scripts/release/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. loader-only no credentials
2. live provider separate
3. SBOM/license/compat/rollback
4. manifest embeds gate

## 必须先写的 RED 测试

- stock Pi rejection/patched host acceptance
- uninstall/reinstall
- no external TS imports

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm release:verify
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

所有 required CI green 后才产 release candidate。

## Evidence

```json
{
  "taskId": "A49",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A49/red.log",
  "greenLog": "artifacts/task-evidence/A49/green.log",
  "fullGateLog": "artifacts/task-evidence/A49/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F103", "F132"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a49): 完成 Hermetic Package/Release Matrix"
```
