# A32 — 建立 v2 Source-witness Locked Corpus

## 元数据

- Wave：`W4`
- Depends on：A07, A17, A20
- Findings：F124
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

独立 cluster、版本锁、不可满足 oracle preflight。

## 允许修改

- `benchmarks/corpus-v2/**`
- `packages/benchmark/src/corpus/**`
- `schemas/oracle*.json`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 30 cluster smoke/100 gate
2. real trace + adversarial
3. train/dev/locked separation
4. major lock

## 必须先写的 RED 测试

- duplicate template detection
- missing witness
- split leakage

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:corpus:verify
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

所有 oracle validity=1，同模板不跨 split。

## Evidence

```json
{
  "taskId": "A32",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A32/red.log",
  "greenLog": "artifacts/task-evidence/A32/green.log",
  "fullGateLog": "artifacts/task-evidence/A32/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F124"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a32): 建立 v2 Source-witness Locked Corpus"
```
