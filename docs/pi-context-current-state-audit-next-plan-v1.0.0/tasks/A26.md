# A26 — 修复 Directive/History/Active-turn 重复注入

## 元数据

- Wave：`W3`
- Depends on：A12, A18, A24
- Findings：F106
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

hard directives 为结构化短消息，原文由 active/recent/evidence层拥有一次。

## 允许修改

- `packages/core/src/materialization/**`
- `packages/runtime/src/runtime-session.ts`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. section source ownership
2. message ID dedup
3. superseded excluded
4. latest user last

## 必须先写的 RED 测试

- same user in history/directive
- compaction summary envelope
- tool pair

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run packages/core/test/materialization/dedup.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

最终 ordered payload 无重复 content hash，latest user完整最后。

## Evidence

```json
{
  "taskId": "A26",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A26/red.log",
  "greenLog": "artifacts/task-evidence/A26/green.log",
  "fullGateLog": "artifacts/task-evidence/A26/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F106"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a26): 修复 Directive/History/Active-turn 重复注入"
```
