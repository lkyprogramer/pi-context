# A22 — Recursive Compaction State Merge

## 元数据

- Wave：`W2`
- Depends on：A12, A18, A20
- Findings：F122
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

第 N 次 checkpoint 从 stores重建，不靠解析 summary。

## 允许修改

- `packages/runtime/src/compaction/**`
- `packages/core/src/compaction/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. active directives/claims/task fronts merge
2. resolved continuity horizon
3. prior checkpoint lineage only
4. source span progress

## 必须先写的 RED 测试

- 3 compactions
- correction after first
- old constraint persists

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/integration/recursive-compaction.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

三次 compaction 后 hard directives/temporal/side effects 全正确。

## Evidence

```json
{
  "taskId": "A22",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A22/red.log",
  "greenLog": "artifacts/task-evidence/A22/green.log",
  "fullGateLog": "artifacts/task-evidence/A22/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F122"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a22): Recursive Compaction State Merge"
```
