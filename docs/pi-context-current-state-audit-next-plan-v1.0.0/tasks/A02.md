# A02 — 建立 hermetic Pi 模型与凭证 Fixture

## 元数据

- Wave：`W0`
- Depends on：A00
- Findings：F101, F103
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

pack/loader contract 默认不依赖真实 `~/.pi`; live provider lane 才使用 secret models.json。

## 允许修改

- `scripts/pack-smoke.mjs`
- `tests/acceptance/**`
- `tests/release/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. 拆分 loader-only fixture 与 provider-live fixture
2. 生成最小 fake provider/model config
3. 真实 provider lane 由显式 env 开启并在无凭证时 skip-not-fail

## 必须先写的 RED 测试

- empty home loader
- fake model config
- live env missing yields skipped

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm test:packed
PCR_LIVE=0 pnpm test:acceptance
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

默认 CI 在空 HOME 通过 clean install；nightly 才访问真实凭证。

## Evidence

```json
{
  "taskId": "A02",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A02/red.log",
  "greenLog": "artifacts/task-evidence/A02/green.log",
  "fullGateLog": "artifacts/task-evidence/A02/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F101", "F103"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a02): 建立 hermetic Pi 模型与凭证 Fixture"
```
