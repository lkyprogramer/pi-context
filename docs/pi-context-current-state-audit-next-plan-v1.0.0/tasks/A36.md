# A36 — 真实 F0/Isolated Reader Lane

## 元数据

- Wave：`W4`
- Depends on：A32, A34
- Findings：F117, F119
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

同模型对 full/candidate context回答隐藏 query。

## 允许修改

- `packages/benchmark/src/reader/**`
- `scripts/benchmark/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. F0 answerability
2. B0/B1/B2 reader
3. seeded executor params
4. no tools/controlled tools lanes

## 必须先写的 RED 测试

- unanswerable abstention
- source witness
- candidate loss

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm benchmark:reader:smoke
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

只在 F0可答样本计算 compressor retention/loss。

## Evidence

```json
{
  "taskId": "A36",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A36/red.log",
  "greenLog": "artifacts/task-evidence/A36/green.log",
  "fullGateLog": "artifacts/task-evidence/A36/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F117", "F119"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a36): 真实 F0/Isolated Reader Lane"
```
