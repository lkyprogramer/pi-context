# A10 — 实现 cursor-scoped Session/Tool Registry

## 元数据

- Wave：`W1`
- Depends on：A09
- Findings：F111, F140
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

移除 lastCursor/lastPointers heuristic，以完整 cursor lookup。

## 允许修改

- `packages/runtime/src/session-registry.ts`
- `apps/pi-context-runtime/src/composition-root.ts`
- `packages/pi-adapter/src/tools/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. key=workspace/session/leaf/lineage/model
2. tool ctx 必须解析唯一 session
3. session shutdown 只关闭对应 owner

## 必须先写的 RED 测试

- two sessions same workspace
- two workspaces
- branch/model switch

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm vitest run tests/acceptance/multi-session*.test.ts
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

并发多会话无 pointer/tool scope 混用。

## Evidence

```json
{
  "taskId": "A10",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A10/red.log",
  "greenLog": "artifacts/task-evidence/A10/green.log",
  "fullGateLog": "artifacts/task-evidence/A10/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F111", "F140"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a10): 实现 cursor-scoped Session/Tool Registry"
```
