# A27 — 稳定前缀 Cache Layout 与遥测

## 元数据

- Wave：`W3`
- Depends on：A13, A25, A26
- Findings：F108
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

稳定/追加/易变分区与 cache SLO。

## 允许修改

- `packages/core/src/materialization/**`
- `packages/runtime/src/telemetry/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. directory/recall移到 volatile尾部
2. continuity stable+delta
3. first-different tokens
4. layout version

## 必须先写的 RED 测试

- no-op request cache stable
- one directive update
- recall injection

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm test:performance -- cache
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

多轮 benchmark cache hit ratio 达预注册目标且 outputHash不受开关影响。

## Evidence

```json
{
  "taskId": "A27",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A27/red.log",
  "greenLog": "artifacts/task-evidence/A27/green.log",
  "fullGateLog": "artifacts/task-evidence/A27/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F108"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a27): 稳定前缀 Cache Layout 与遥测"
```
