# A16 — 实现真实 Restart/Branch Recovery

## 元数据

- Wave：`W1`
- Depends on：A11, A12, A15
- Findings：F112
- Owner：由 `taskctl claim` 分配
- Completion：必须同时有 RED、GREEN、FULL-GATE、Evidence Seal、Commit SHA

## 目标

reconcile Saga、receipts、checkpoint ack、candidate stale。

## 允许修改

- `packages/runtime/src/recovery/**`
- `packages/storage-node/src/saga-store.ts`
- `tests/fault/**`

禁止修改未列出的生产文件；若发现依赖缺口，写 Blocker，不得扩大 Scope。

## 实施步骤

1. session_start catch-up
2. branch switch new scope
3. orphan prepared/host-visible/committed matrix
4. idempotent replay

## 必须先写的 RED 测试

- crash at blob/saga/host ack
- branch rewind
- duplicate event

RED 必须因目标缺陷失败，而不是 import/path/fixture 错误。

## 命令

```bash
pnpm test:fault -- recovery
```

## 负例与故障要求

- scope/model/branch 不匹配必须 typed fail；
- AbortSignal 必须在 I/O/模型边界生效；
- 不得通过 hardcode 指标或 snapshot 常量过 Gate；
- 不得修改 locked corpus 同一 major 来修复实现失败；
- 所有临时目录使用 OS temp fixture。

## 验收

Recovery report actions 非空且与 fixture oracle 一致。

## Evidence

```json
{
  "taskId": "A16",
  "status": "done",
  "commit": "<40-hex>",
  "redLog": "artifacts/task-evidence/A16/red.log",
  "greenLog": "artifacts/task-evidence/A16/green.log",
  "fullGateLog": "artifacts/task-evidence/A16/full-gate.log",
  "sourceDigest": "<sha256>",
  "runBundleHashes": [],
  "findingsClosed": ["F112"]
}
```

## Commit

```bash
git add -- <allowed files + evidence>
git commit -m "fix(a16): 实现真实 Restart/Branch Recovery"
```
