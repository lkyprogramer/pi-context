# AI START HERE

1. 读 `00-executive-summary.md`、`findings/FINDINGS.md`。
2. 运行 `python3 scripts/validate_artifacts.py`。
3. 运行 `python3 scripts/taskctl.py next`。
4. 从 W0 开始，不跳过 CI Truth。
5. 任务完成前不要修改状态文件为 done；Evidence seal 与 commit 是唯一完成依据。
6. 当前默认结论是 `keep-pi-native`，任何 adopt 必须由 A48 Gate 自动生成。
