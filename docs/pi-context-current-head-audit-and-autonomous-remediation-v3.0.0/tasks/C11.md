# C11: 把 Recall Lease 持久化并实现 TTL/次数/撤销

**Wave:** W2  
**Depends on:** C10  
**Findings:** CF-026, CF-027

## Goal

把 Recall Lease 持久化并实现 TTL/次数/撤销，并形成可复算 Evidence v3。

## Allowed Files

- `packages/storage-node/src/index.ts`
- `packages/runtime/src/index.ts`
- `apps/pi-context-runtime/src/composition-root.ts`
- `packages/storage-node/test/recall-lease-store.test.ts`
- `packages/runtime/test/proactive-recall.test.ts`

## Interfaces


- Consumes: 当前仓库 public contracts、Pi 0.84.4 pinned patch、上一任务输出。
- Produces: 可由后续任务直接消费且由 strict TypeScript 类型约束的接口或证据产物。


## Acceptance

实现要求：SQLite `recall_lease` 使用条件更新消费；TTL 到期、remainingUses=0、wrong cursor、restart、concurrent consume 都有测试。

## Commands

  - `corepack pnpm vitest run packages/storage-node/test/recall-lease-store.test.ts packages/runtime/test/proactive-recall.test.ts`

## Steps


- [ ] **Step 1: 记录 RED** 运行首条命令并把非零退出码和完整日志写入 Evidence v3；若当前已绿，先加入能复现本 Finding 的最小失败测试。
- [ ] **Step 2: 实施最小改动** 只修改 Allowed Files，不顺手重构无关模块。
- [ ] **Step 3: 运行 Narrow GREEN** 要求列出的 narrow commands 全部 exit 0。
- [ ] **Step 4: 运行 Full Gate** 至少执行 `corepack pnpm typecheck`；涉及 package/CI 的任务还必须执行 `corepack pnpm test:packed`。
- [ ] **Step 5: 生成 Evidence v3** 保存命令、退出码、log SHA-256、artifact SHA-256、base/head commit 和验收断言。
- [ ] **Step 6: Commit** 使用 `fix(<task-id>): <title>`，一个任务一个可回滚提交。
