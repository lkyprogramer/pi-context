# C24: 完成 current-head 100×3 四臂权威运行

**Wave:** W4  
**Depends on:** C15, C17, C18, C19, C20, C21, C22, C23  
**Findings:** CF-010

## Goal

完成 current-head 100×3 四臂权威运行，并形成可复算 Evidence v3。

## Allowed Files

- `tests/live-gate/paired-w2-live.ts`
- `artifacts/runs/w2-v4-live/paired-gate/run-manifest.json`

## Interfaces


- Consumes: 当前仓库 public contracts、Pi 0.84.4 pinned patch、上一任务输出。
- Produces: 可由后续任务直接消费且由 strict TypeScript 类型约束的接口或证据产物。


## Acceptance

实现要求：不得复用旧 a6281b3/0e684e3 row；新目录 w2-v4-live；planned=300；所有失败留在 denominator；报告列出每个 timeout。

## Commands

  - `PCR_LIVE=1 PCR_W2_LIVE_PROFILE=gate corepack pnpm tsx tests/live-gate/paired-w2-live.ts`
  - `node scripts/release/verify-w2-live.mjs artifacts/runs/w2-v4-live/paired-gate`

## Steps


- [ ] **Step 1: 记录 RED** 运行首条命令并把非零退出码和完整日志写入 Evidence v3；若当前已绿，先加入能复现本 Finding 的最小失败测试。
- [ ] **Step 2: 实施最小改动** 只修改 Allowed Files，不顺手重构无关模块。
- [ ] **Step 3: 运行 Narrow GREEN** 要求列出的 narrow commands 全部 exit 0。
- [ ] **Step 4: 运行 Full Gate** 至少执行 `corepack pnpm typecheck`；涉及 package/CI 的任务还必须执行 `corepack pnpm test:packed`。
- [ ] **Step 5: 生成 Evidence v3** 保存命令、退出码、log SHA-256、artifact SHA-256、base/head commit 和验收断言。
- [ ] **Step 6: Commit** 使用 `fix(<task-id>): <title>`，一个任务一个可回滚提交。
