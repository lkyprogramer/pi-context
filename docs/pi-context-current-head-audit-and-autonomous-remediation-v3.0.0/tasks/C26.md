# C26: 拆分 Overflow Prevention 与 Forced Recovery

**Wave:** W4  
**Depends on:** C19, C25  
**Findings:** CF-029, CF-030

## Goal

拆分 Overflow Prevention 与 Forced Recovery，并形成可复算 Evidence v3。

## Allowed Files

- `tests/live-gate/w5-live-lanes.ts`
- `tests/live-gate/forced-overflow-provider.ts`
- `tests/fault/overflow-lane.test.ts`

## Interfaces


- Consumes: 当前仓库 public contracts、Pi 0.84.4 pinned patch、上一任务输出。
- Produces: 可由后续任务直接消费且由 strict TypeScript 类型约束的接口或证据产物。


## Acceptance

实现要求：Prevention 与 Recovery 两份报告；Forced Recovery 必须捕获可分类 context_length error，然后一次 compact/retry，且 side-effect count 不增加。

## Commands

  - `corepack pnpm vitest run tests/fault/overflow-lane.test.ts`
  - `PCR_LIVE=1 corepack pnpm tsx tests/live-gate/w5-live-lanes.ts overflow`

## Steps


- [ ] **Step 1: 记录 RED** 运行首条命令并把非零退出码和完整日志写入 Evidence v3；若当前已绿，先加入能复现本 Finding 的最小失败测试。
- [ ] **Step 2: 实施最小改动** 只修改 Allowed Files，不顺手重构无关模块。
- [ ] **Step 3: 运行 Narrow GREEN** 要求列出的 narrow commands 全部 exit 0。
- [ ] **Step 4: 运行 Full Gate** 至少执行 `corepack pnpm typecheck`；涉及 package/CI 的任务还必须执行 `corepack pnpm test:packed`。
- [ ] **Step 5: 生成 Evidence v3** 保存命令、退出码、log SHA-256、artifact SHA-256、base/head commit 和验收断言。
- [ ] **Step 6: Commit** 使用 `fix(<task-id>): <title>`，一个任务一个可回滚提交。
