#!/usr/bin/env node
/**
 * historical: superseded by eval/local (E03). Not a current eval entry.
 */
console.error("historical: eval/smoke.mjs was the v5 B0–B3 runner; use eval/local/run-matrix.mjs");
process.exit(2);
