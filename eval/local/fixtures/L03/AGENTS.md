# Long Agent case
- This mimics a Java runtime incident investigation.
- Read the incident log and relevant design notes before editing.
- Tests and verify.sh are protected.
- Do not hard-code expected test values.
- Preserve public APIs.
- Run `./verify.sh` after changes and continue debugging until it passes.
