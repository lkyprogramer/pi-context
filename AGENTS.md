# pi-context 开发规范

## 默认原则

这是个人项目，默认目标是快速完成正确的开发。先改代码，再用最窄的测试证明行为；不要把发布审计流程套在每个普通改动上。

## 两条工作路径

### 开发路径（默认）

适用于功能、重构、一般缺陷修复和测试调整。

1. 先阅读直接相关的实现、调用方和现有测试。
2. 采用最小完整改动；相关文件可以在同一批次内一起修改。
3. 运行与改动直接相关的定向测试；类型、公共合同或跨模块改动再运行 `typecheck`、对应 contract/integration 测试。
4. 一个完整模块或一组相关改动完成后，统一做一次 review、回归和提交。
5. 提交信息描述最终改动，不为每个小修复单独提交。

开发路径不要求：独立 worktree、RED 日志、每条命令的 SHA、Evidence v3、GitHub API、branch protection、发布 bundle 或人工 taskctl 登记。

### 发布/审计路径（按需）

只有以下情况才进入：真实 Provider/live 基准、300 gate、发布包、数据库/数据迁移、安全敏感边界、用户明确要求 Evidence v3，或准备发布/部署。

此路径才要求完整的 run manifest、输入和输出 hash、日志留存、reviewer、外部门禁和 fail-closed 发布判定。审计文档中的 Allowed Files、真实数据来源和 claim 级别必须遵守；不能用开发路径的单测替代真实发布证据。

## 测试选择

- 纯局部逻辑：运行单个相关测试文件。
- 模块行为：运行该模块测试和必要的 typecheck。
- 公共合同/跨模块：运行 contract 或 integration。
- 发布包、入口、依赖或运行时：运行 acceptance/packed。
- 全仓 `test:unit`、完整 gates 和 live 基准只在共享基础设施、发布前或风险确实覆盖全仓时运行。

测试失败时修复根因；不要放宽断言、删除失败样本或把未知值改成零。

## Review 与提交

普通开发不要求每个小改动派代理。达到一个 W、一个完整模块或用户明确要求 review 时，派只读 review 子代理核对规格、范围和回归风险；发现问题先修复，再运行相关测试。

提交按功能批次进行。不要为了记录过程频繁提交；但安全修复、需要隔离的回滚点或用户明确要求时可以单独提交。

## 证据和声明

区分三种结论：代码已实现、测试已通过、真实环境已验证。前两者不能自动推出第三者。

没有真实 corpus、Provider、GitHub 或生产权限时，保留 fail-closed 和明确的未验证状态，不伪造发布结论。300 gate 是最终发布验证，不是日常开发前置条件。

## 安全边界

- 凭据只放本地 `.env` 或用户配置，不写入代码、日志、manifest、提交和回复。
- 不执行 push、deploy、publish、生产数据写入，除非用户明确授权。
- 保留用户已有的 live 产物、partial、UNRUN 和未跟踪文件；删除或覆盖前先确认范围。
