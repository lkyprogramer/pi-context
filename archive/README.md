# Archive (not the product)

`pcr-tests/` and `pcr-scripts/` are the removed PCR 0.84.4 workspace leftovers. They are not part of `pnpm test` or the v5 packed plugin.

Do not treat passing or failing these files as a v5 gate.
